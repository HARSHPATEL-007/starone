## Objective
- Keep enhancing the N0VA Ads & Marketing module for day-to-day execution with fewer user clicks. Rounds 1-14 complete (quick-views, batch actions, daily dashboard, two one-click execution layers, set-and-forget layer, silent-guardian layer, Command Center page, help/contextual support, keyboard shortcuts, mobile-first, mobile viewport verified), **Rounds 15-18 built the N0VA MAIL module** (Round 15: core mail engine backend; Round 16: 5 mail frontend pages; Round 17: contacts book, N0VA1O agent, compliance, voice + 3 pages; Round 18: templates + bulk send, signatures, spam/quarantine, snooze/follow-ups, attachments, analytics — 6 services + 6 pages + 46 tests green) AND **Round 19 implemented 5 remaining spec sections — real-time collaboration, predictive intelligence, bulk campaign agent, eDiscovery, custom domains — 5 services + 5 pages + 52 tests green** AND **Round 20 added voice & multimodal messaging (§3.9), AI-assisted rule generation, batch message operations, and the Neural Mailbox (§3.2.2) — 3 new services + 2 extended services + 1 page (MailVoice) + neural section in MailAI + AI-rule modal in MailRules + 48 tests green** AND **Round 21 added Mail Command Center — daily mail execution dashboard with one-click actions (rules sweep, spam rescan, agent cycle, smart archive, follow-up completion, send-scheduled-now, campaign approve/reject), sweepAll rules across the inbox, MailInbox batch selection toolbar, MailRules Run-now button, Sidebar badge refresh on route change — 1 new service + 2 extended services + 1 page + 23 tests green** AND **Round 22 added Mail Ops & Incident Response (§8.1/§8.2) + Storage Tiering & AI Cleanup (§3.3.1/§3.3.2) — monitoring dashboard (health/performance/security/business + alerts), P0-P4 incident lifecycle (file/escalate/acknowledge/resolve), one-click housekeeping/retry/checkpoint/threat-rule, hot→cold tiering with auto-tier policy, AI cleanup suggestions (newsletters/large attachments/duplicates), storage forecast — 2 new services + 1 page + 29 tests green** AND **Round 23 added Search Operators (§3.4.2) + Anti-Abuse Engine (§2.4) — 15-operator query language with parse explanations/invalid feedback/recent history/coverage stats (MailSearch panel), five-layer defense engine (network/protocol/content/behavioral/quantum) with deterministic scan verdicts (deliver/quarantine/reject/greylist), rate limiting with burst detection, BEC/DLP scanning, impossible travel, sending-pattern anomaly, honeypots, one-click threat responses, defense events log (MailProtection page) — 2 new services + 2 pages + 37 tests green**. ALL spec sections §1-§14 now implemented, `npm run build` is GREEN, the full backend suite is GREEN (2360/2360 across 99 files), and every page in the app renders cleanly at 375×812 with zero horizontal overflow. Remaining: N0VA MAIL polish + later spec sections (see Next). AND **Round 24 added Mail Webhooks (§4.4) + the N0VA1O Mail Agent Framework (§11) + 5 new search operators — query language extended 15→20 (`has:voice`, `has:poll`, `collaborated:with:EMAIL`, `ai:suggested`, `visual:contains:`), webhook registry (11-event catalog, CRUD subscriptions w/ `whsec_` secrets, HMAC-signed deterministic deliveries, delivery log + stats + overview), agent registry (10 personas, 25 tools, intent-based discovery, `n0va_ag_` API keys, sessions, HITL approval queue + resolve, audit log, dashboard) — 2 new services + 1 extended service + 2 pages (MailWebhooks, MailAgents) + 45 tests green** AND **Round 25 added the Mail Integrations hub (§9) - 10-connector catalog (gmail/outlook/slack/teams/crm/drive/dropbox/zoom/calendar/notion), connection lifecycle (connect w/ reconnect semantics, authorize/refresh/update/disconnect), deterministic sync jobs (syncNow/syncHistory/syncStatus), 9 provider actions (push_to_crm, post_to_chat, upload_file, schedule_meeting, create_event, create_task, sync_contacts, forward_to_channel, sync_mail), 7-event automations (bridges), alerts + overview + logs - 1 new service + 1 page (MailIntegrations) + 37 tests green** AND **Round 26 added Mail Billing & Quota Management (§3.3) + a Mail Notifications Center — 4-plan catalog with 9 limit dimensions (mailboxes/storage/contacts/messagesPerDay/domains/webhooks/agents/campaigns/integrations), usage metering + quota warnings, upgrade w/ prorated invoices + pay, payment methods, storage forecast, billing log/dashboard, 12-type notification center with dedupe + per-type settings, 11-source alert collector (follow-ups/snoozed/campaigns/spam/storage/incidents/webhooks/domains/agent-HITL/integrations/invoices), mark-read/read-all/clear, notification center page — 2 new services + 2 pages (MailBilling, MailNotifications) + 33 tests green (full suite 2430/2430 across 101 files)** AND **Round 27 added the Mail Dispatch execution page — one-screen view combining queued scheduled sends (create + Send-now + cancel), open follow-up reminders (complete/delete), and campaign send windows (launch/approve/reject/pause/resume) with stat cards + totals chips — 1 new page (MailDispatch) + 0 backend changes** AND **Round 28 added the Mail Calendar page — monthly execution timeline (42-cell grid, prev/next/Today nav, color-coded legend) combining scheduled sends, follow-up reminders, campaign send windows and snoozed messages with per-day detail panel + one-click actions (send-now/cancel/done/approve/reject/pause/resume/unsnooze) — 1 new page (MailCalendar) + 0 backend changes** AND **Round 29 added the Mail Reader page — split-view threaded reading pane (list + reader side-by-side on desktop, pane swap on mobile) with inline reply/forward, AI smart-reply chips, swipe-to-archive/trash gestures and J/K keyboard nav — 1 new page (MailReader) + 0 backend changes** AND **Round 30 completed the remaining §3.3 billing surface — add-ons storefront (6 items, prorated charges + refund credits), overage policy (warn/block/bill with projected costs + invoicing), enterprise term contracts (12/24/36 months, discounted annual pricing, effective monthly rate), usage alerts (critical/warning + thresholds), downgrade with prorated refunds (blocked when usage exceeds target plan), auto-renew toggle, cancel subscription, credit balance — 1 extended service + 1 extended page + 26 tests green (full suite 2456/2456 across 102 files)** AND **Round 31 added an eDiscovery & Compliance deep-dive — legal hold calendar with start/end dates, concept search clustering, privilege log, hash-verified export audit chain, GDPR/CCPA/HIPAA compliance report packs with exports that join the chain — 2 extended services + 2 extended pages + 19 tests green (full suite 2475/2475 across 103 files)** AND **Round 32 added quantum-safe mail security (PQC key pairs, QKD channel simulation, post-quantum encryption, certificate chain) + Collaboration v2 (approval workflows, inbox delegation, team roles, @mentions) — 2 new services + 1 new page + 1 extended page + 31 tests green (full suite 2506/2506 across 104 files)** AND **Round 33 added Mail OAuth 2.0 (§9) + the Mail Migration Assistant — OAuth lifecycle (start w/ authorization URL + pending state, callback w/ state-match + oat_/rft_ tokens, refresh that never shortens expiry, revoke, status), 3 new provider actions (share_link, update_crm_deal, create_doc), migration wizard (start w/ 4 providers × 3 modes + per-folder weights summing to total, scan w/ sizes, preview w/ folder mapping junk/bulk→spam + dedupe estimate, import w/ messageId dedupe + imported flags, progress/remaining, delete, log, summary) — 1 extended service + 1 new service + 1 new page (MailImport) + OAuth modal + chips in MailIntegrations + 30 tests green (full suite 2536/2536 across 105 files)**. AND **Round 34 added Mail Thread Workspaces (unified multi-message conversation views: threadList w/ 9 filters incl. tag/priority/unread/starred/pinned/search, threadWorkspace w/ participants/messageCount/unreadCount/state/priority/tags/suggestedNext, setThreadState incl. done→marks unread read, pin/unpin, tag/untag, priority, mergeThreads re-threading target+source messages, threadDashboard byState/byTag/byPriority/topTags, threadLog) + connector catalog 10→15/6→7 categories (google_sheets, asana, whatsapp, xero, drive + read_from_drive added) + 6 new integration actions (push_to_sheets/create_ticket/post_comment/send_sms/create_invoice/read_from_drive) + billing coupons (4-code catalog w/ per-code remaining/maxUses, apply/remove/status, 10% off discount applied in buildInvoice) + tax rates (DEFAULT_TAX_RATE 8, setTaxRate 0-30, tax computed on discounted subtotal) — 3 extended services + 1 new page (MailThreads) + integrations/billing pages extended + 34 tests green (full suite 2570/2570 across 106 files)**. AND **Round 35 added the Mail Automation Studio + Deliverability Center, Nurture sequences + Unsubscribe center, Team ticketing, and Backup & restore + Mail API keys — 7 new services (MailAutomationService w/ AUTOMATION_TRIGGERS 6/ACTIONS 9, createAutomation w/ multi-step normalize + delayHours scheduling to mail_automation_runs, runAutomation/testAutomation w/ stepResults + wouldRun, dueRuns, dashboard, log; MailDeliverabilityService w/ BOUNCE_CLASSES 3/SUPPRESSION_REASONS 4/REPUTATION_LEVELS 3, recordBounce w/ auto-suppress on hard/complaint, bounceStats/listBounces, suppressEmail/unsuppressEmail/suppressionList/status, sendingReputation (base 85 + hash%11 —5/hard —10/complaint, min 5 max 100), listHygiene, deliverabilityDashboard w/ alerts; MailSequenceService w/ createSequence (steps w/ delayHours), listSequences, enrollContact (nextRunAt = now + first delay)/enrollMany/pause/resume/unenroll, advanceSequence (due enrollments only, sends via composeSend), sequenceProgress, sequencesDashboard; MailUnsubscribeService w/ generateLink (
0va.io/unsubscribe/<token>?cat=), unsubscribe, preferences get/update, unsubscribeStatus, log, dashboard; MailTicketService w/ createTicket (slaDeadline from slaHours)/listTickets (status/priority/assignee/tag/search filters)/update/assign/priority/tags/notes, resolve/reopen/escalate, slaOverview (overdue/due_soon/healthy/done), ticketDashboard w/ avgResolutionHours, ticketLog/events; MailBackupService w/ createBackup snapshot (mailboxes/messages/contacts/threads/rules/automations/sequences/templates/campaigns/tickets, sizeBytes via estimateMessageBytes, 10-newest cap), backups/backup/restore (stamps restoredAt + restoredCount)/delete, backupSchedule GET/PUT (autoBackup/intervalHours/retentionDays), backupsDashboard; MailApiKeyService w/ createApiKey (
0va_mk_ prefix + hashed key + prefix/last4, scopes send/read/webhook/campaigns, throws on empty scopes), keys/get/revoke, verifyApiKey (valid/reason revoked/invalid), recordApiCall w/ per-key + mail_api_key_usage rows, usage w/ byAction, dashboard — collections mail_automations, mail_automation_log, mail_automation_runs, mail_bounces, mail_suppression, mail_preferences, mail_unsubscribes, mail_unsubscribe_links, mail_sequences, mail_sequence_enrollments, mail_tickets, mail_ticket_events, mail_backups, mail_backup_policy, mail_api_keys, mail_api_key_usage) + 5 frontend pages (MailAutomations /mail/automations w/ trigger+action catalogs, run-against-message + test modals w/ stepResults, multi-step create modal; MailDeliverability /mail/deliverability w/ reputation bar + alerts + bounce log w/ type filter + suppression list w/ undo + list hygiene + record-bounce/suppress modals; MailSequences /mail/sequences w/ sequence cards + step progress + advance/enroll/toggle/delete + unsubscribe section w/ link generator + preferences lookup/save + opt-out log; MailTickets /mail/tickets w/ status/priority filters + search + SLA badges + file modal + detail modal w/ resolve/reopen/escalate/priority/assign/tags/notes; MailAdmin /mail/admin w/ backups list + restore/delete + schedule form + back-up-now modal + API keys w/ create (key revealed once + copy)/verify/usage/revoke) — 7 services + ~77 routes + ~90 client methods + 41 tests green (full suite 2611/2611 across 107 files)** AND **Round 36 added Mail Biometrics, Zero Trust, AI Governance, Performance & Chaos Engineering — backend (5 services + `NovaMailRound18.test.ts` 35 green ×4, full suite 2646/2646 across 108 files: `MailBiometricService` (`mailBiometric`) w/ BIOMETRIC_SIGNALS 8 (keystroke/mouse/face/voice/gait/eye/geo/cadence), recordSample/baseline w/ risk + avgTrustScore, evaluateSession (trustScore 0-100 + anomalyCount + riskLevel), session lifecycle, biometricLog; `MailZeroTrustService` (`mailZeroTrust`) w/ ZT_LAYERS 6 (device/identity/session/data/network/quantum) + ZT_PRINCIPLES 4 (never trust/least privilege/micro-segmentation/continuous verify), enforceAccess (device risk + posture passRate → allow/challenge/deny), enrollDevice/posture, honeytokens CRUD + hit detection, zeroTrustOverview/Dashboard/Log; `MailAiGovernanceService` (`mailAiGovernance`) w/ MODEL_DOMAINS 4 + ETHICS_COMMITTEE 5 (EXPORTED from the service — Round 18 test import fix), registerModel/reviewModel (approve/reject), human-in-the-loop guardrails (inputSanitization/outputFiltering/rateLimiting/auditTrail), scanInput (injection patterns + prompt-injection verdict) / scanOutput (toxicity/PII/hallucination risk), shadowAi, redTeam (deterministic attacks), governanceDashboard/Log; `MailPerformanceService` (`mailPerformance`) w/ CACHE_LAYERS 9 (L1-L9 w/ hitRateTarget) + SCALABILITY_TARGETS 8 + EDGE_REGIONS + GREEN_CERTS, caching (avg hit rate + on_target/below_target), flushCache, explainQuery (indexMatches/estimatedMs/verdict), queryOptimization (7 index techniques), scalability (currentLoadPct/headroomPct/verdict), edge (regions w/ latencyMs/loadPct), sustainability (greenScore/greenLevel/initiatives/certifications), performanceDashboard/Log; `MailChaosService` (`mailChaos`) w/ CHAOS_EXPERIMENTS 8 (smtp_kill/mongo_failover/ai_corruption/cdn_flush/ws_partition/quantum_compromise/voice_degradation/ddos_sim, 6 autoRollback) + GAME_DAYS 4, runExperiment (recoverySeconds 4-74, status recovered/failed, runId = inserted._id), abortExperiment, resilienceScore (recovered/total × 100 + level), gameDays (lastRun/nextRun), chaosDashboard (catalog + runs + resilience + gameDays + recentEvents), chaosLog) + frontend (5 pages: `MailBiometrics.tsx` /mail/biometrics w/ 4 stat cards + signals grid w/ deviation bars + baseline card w/ risk level + session rows w/ trustScore chips + recent events; `MailTrust.tsx` /mail/zero-trust w/ ZT score gauge + posture badge + layer bars (passRate/verifiedCount/blockedCount) + 4 principles + device rows w/ risk chips + honeytoken list + enroll modal + evaluate modal (deviceId + action); `MailGovernance.tsx` /mail/governance w/ stat cards + model cards w/ bias audit + register modal + review Approve/Reject + guardrails grid + scanner card w/ sample payload buttons (injection/PII/toxic/clean) + ethics committee + red-team button w/ contained banner; `MailPerformance.tsx` /mail/performance w/ 4 stat cards + cache layer rows (hit-rate bars + ttl + hits) + Flush all + query explainer w/ index chips + edge regions + scalability targets + sustainability gauge + certifications; `MailChaos.tsx` /mail/chaos w/ resilience score banner + 4 stat cards + experiment catalog w/ per-card Run button (mailChaosRun) + run history + game-day cadence + events log — all registered in App.tsx (lazy routes after /mail/admin) + Sidebar (Fingerprint/Shield/Scale/Gauge/Beaker icons) + CommandPalette; tsc ZERO errors, `npm run build` GREEN (~8.4s), browse-verified 375×812 zero doc overflow zero errors — only benign `truncate` spans + pre-existing React Router/socket.io warnings, mobile nav visible, chaos Run clicked through the UI (resilience 100/100, 1/1 recovered))**. AND **Round 37 added Mail Focus & Zero-Click Intelligence (Day-to-Day Execution Guide §6.2 Focus Mode, §6.1 Notification tiers, §6.3 Smart Batching, §4.3 Batch Intelligence, §12.2 Daily Friction Score, §12.3 ROI Calculator) — 1 new service (MailFocusService mailFocus w/ FOCUS_MODES 5 incl. open-ended vacation, NOTIFICATION_TIERS 4, BATCH_SUGGESTIONS 6, activateFocus/focusStatus/endFocus/extendFocus, notificationTiers, smartBatch, batchIntelligence + executeBatchSuggestions, frictionScore, roiCalculator, focusLog/focusDashboard; collections mail_focus_sessions, mail_focus_log, mail_friction) + 17 delegations + 16 routes + 17 client methods + 1 page (MailFocus at /mail/focus) + MailInbox pull-to-refresh gesture + NovaMailRound19.test.ts 24 green ×3 (full suite 2670/2670 across 109 files)**. AND **Round 38 added the Neural Email Composer (§3.3 Smart Compose — zero-tap drafting + roadmap Q3-2026 "In Development" item) — 1 new service (`MailComposerService` `mailComposer` w/ COMPOSER_MODES 3 (reply/forward/new) + COMPOSER_TONES 3 (concise/warm/formal) + COMPOSER_INTENTS 6 (contract/meeting/question/invoice/request/update w/ keyword detection), composerCatalog, composeDraft w/ deterministic full-body generation (greeting from sender first name, 2-4 section verdict points ✅/⚠️, intent-specific opener, call-to-action, sign-off; basedOn "your past N replies + calendar availability"; confidence 70-99; actions send/edit/regenerate/don't_like; clicksToSend 1), regenerateDraft (variant+1 fresh body), dislikeDraft (feedback + auto-regenerate), saveEdits, sendDraft (1-click via mailMessage.composeSend + status sent), listDrafts/getDraft/deleteDraft, composerDashboard (draftsTotal/sent/dislikes/regenerations/avgConfidence/topIntents/actions/recentLog), composerLog; collections mail_composer_drafts, mail_composer_log; determinism seed `${from.email}|${subject}|${mode}|${tone}|${variant}` — never genId ids (Round 19 lesson)) + 11 delegations + 11 routes (`/mail/composer/*` — catalog/dashboard/drafts/log static before `:draftId`; POST draft, drafts/:draftId regenerate|dislike|edits|send, GET+DELETE drafts/:draftId) + 12 client methods + `NovaMailRound20.test.ts` 19 tests green stable 3× (full suite 2689/2689 across 110 files; tenant nova-mail20 with 7 intent fixtures incl. one whose body avoids the word "contract" so request-intent detection stays deterministic) + frontend page `MailComposer.tsx` at /mail/composer (4 stat cards, zero-tap compose form w/ inbox-message select + mode/tone selects, draft preview w/ ✅/⚠️ sections + basedOn basis + Send (1 click)/Regenerate/Edit/Don't-like buttons, intent chips + recent activity, drafts list w/ status chips + open/send/delete, 30s auto-refresh + `n0va:refresh-data` listener) registered in App.tsx (after /mail/focus) + Sidebar ("Mail Composer", PenLine icon — new import) + CommandPalette (PenLine added) — tsc ZERO errors both sides, `npm run build` GREEN (~14.8s), backend tsc no NEW errors, live smoke (catalog 3/3/6, draft on Q3 invoice → contract intent 84% warm tone "Hello John", regenerate v2, dislike v3 w/ feedback, send 1-click → sent folder, dashboard 2 drafts/2 sent), browse-verified 375×812 (sw 375 == cw, overflowEls [], mobile nav visible, message select populated w/ 13 inbox msgs, zero-tap draft rendered in preview, Send clicked through the UI → sent folder 2 Re: messages) + 1366×768 clean, AGENTS.md updated.**

## Important Details
- Pattern per feature: service method → delegation in `AdsMarketingModuleService.ts` → route in `ads-marketing-module.ts` → client method in `client.ts` → vitest file.
- Run tests from `C:\Users\hvipa\Downloads\New NOVA\starone\n0va\packages\backend`: `npx vitest run <file>` (no `--timeout` flag) or `npx vitest run` for full suite (~13-20s).
- Full suite state: **100 files — ALL PASSED; 2397 passed / 0 failed (2397 total)**. The 22 pre-existing DeeperMarketingServices.test.ts failures were fixed (Round 12) by adding missing singleton exports. Do not remove those exports. **Fixed a pre-existing flake in `WorkflowBuilderService.saveWorkflow` (Round 15): id was `wf_${Date.now()}` only — two saves in the same ms collided and `getWorkflow` returned the earlier paused workflow → "Workflow must be active or draft". Now `wf_${Date.now()}_${random6}` (same pattern as the seed/exec ids). Keep the suffix.**
- **Round 19 flake fix (IMPORTANT, keep): `MailCampaignService.executeCampaign` keyed all deterministic rolls on `hashStr(email + campaign._id)` — but MemoryStore `genId()` embeds `Date.now()`, so campaign ids differ every run → complaint/fail/deliver/open rolls were random → NovaMailRound4 flaked (~50% of runs). Fixed: stable seed string `${name}|${templateId}|${JSON.stringify(audience)}|${abSubject||""}` (all immutable campaign fields); rolls now `hashStr(email + "|" + seed [+ "del"/"open"/"click"/"reply"/"complaint"])`. ALSO: test fixtures must not feed ids into hashes — `campaignTemplateId` is now a pinned direct insert `_id: "tpl_round4_campaign"` instead of `createTemplate()` (whose genId varied per run). Verify determinism by running NovaMailRound4 5-8×.** The "Big blast" (60-recipient bulk) seed deterministically auto-pauses on approve (complaint rate >2%) — the approve test asserts `status "paused"` + `stats.complaints > 0` as the auto-pause coverage.
- Backend `npx tsc --noEmit -p tsconfig.json` still has many PRE-EXISTING errors repo-wide (e.g. CampaignExperimentation.test.ts `plannedEndDate`; AdsMarketingModuleService import/export issues for PortfolioBudgetOptimizerService/CampaignInsightsEngineService/CampaignIssueService). Vitest is unaffected. Fix only NEW tsc errors.
- Frontend `npx tsc --noEmit` (from `packages/frontend`): **ZERO errors (Round 12 made it green)**. Fixed 10 TS1117 duplicate keys in `client.ts` (merged `creativeAI` 519+1055, `audienceInsights` 825+1066 — kept the enhanced `lookalike(seed, pool, size)`, dropped the unused bare `lookalike()`; removed 7 duplicate `dsAlgorithms` methods keeping the earlier generic `(data)` signatures since DSAlgorithms.tsx passes `{ params }`; removed duplicate `segmentOverlap` in adsMarketingModule) plus 5 page errors (CreativeAIEnhanced missing `TrendingDown` import; AudienceInsightsEnhanced implicit-any map callback; ForecastingDashboard `showToast`→`addToast(type, title)` ×6 + `forecast?` optional + guarded `.forecast?.length`). `npm run build` (tsc && vite build) is now GREEN.
- IMPORTANT envelope convention: backend routes wrap every response in `sendSuccess(res, data)` → body is `{ data: {...} }`; the frontend `request()` helper does NOT unwrap it. **Round 14 lesson: list endpoints return `{data: [...]}` and any page that does `r.filter/map/reduce` on the raw response crashes with "X is not a function". Universal fix pattern (used across ~30 pages): `Array.isArray(r) ? r : r?.data || []` (campaigns search variant is `r?.campaigns`; ReportBuilder metrics read `m?.data?.metrics ?? m?.metrics`).** When adding a new page that calls any list endpoint, apply this unwrap in the loader — do NOT set state to the raw response.
- Round 14 shape mismatches to remember: backend `segmentation` rows are `{_id,name,type,criteria,status,size,createdBy,createdAt}` — page must map `groups: seg.groups||[]`, `estimatedSize: seg.estimatedSize ?? seg.size ?? 0`, `description: seg.description||""` (Segmentation.tsx `normSeg`); backend `landing-pages` rows are `{title,slug,content,templateId,campaignId,status}` (NO `views`/`conversions`/`tags`/`name`/`url`) — LandingPages.tsx `normLp` maps `name←title`, `views/conversions: 0`, `tags: []`, `url: "/"+slug`, `id←_id`. Browse-verified at 375×812.
- `api.adsMarketingModule.*` is the ONLY client namespace wired into pages — `CommandCenter.tsx` calls commandCenterSummary, dailyExecutionDashboard, crossPlatformPerformance, autopilotStatus, audienceSyncStatus, fraudProtectionStatus, attributionReport; `HelpDialog.tsx` calls quickFixes/applyQuickFix/fixAll — each with `.catch(() => null)`. Auth: JWT HS256 dev secret in `backend/src/middleware/auth.ts`; dev token minted via `jwt.sign({userId, tenantId, role}, JWT_SECRET)` (jsonwebtoken in backend node_modules). Live API is mounted at `/api/v1/ads-marketing-module` and REQUIRES header `x-tenant-id` (else 400). Dev token cached at `C:\Users\hvipa\AppData\Local\Temp\opencode\n0va_token.txt`.
- Seed fixtures with `DataStore.mem().insert(...)`. MemoryStore API: `insert`, `find(collection, predicate)`, `findOne`, `update(collection, predicate, patch)`, `delete(collection, predicate)` (used by mailSearchOp.clearHistory).
- Collection stores used: `audiences`, `audience_actions`, `autopilot_config`, `autopilot_log`, `approval_settings`, `approval_decisions`, `triage_logs`, `launched_templates`, `workflow_log`, `creatives`, `content_assets`, `brand_safety_log`, plus existing `campaigns`/`metrics`.
- Deterministic values via local `hashStr` helper (NOT Math.random) so tests never flake. qualityScore range is `hash % 100` — can produce low_quality; to force auto-pause in tests, seed an audience with `qualityScore: < 30` directly.
- Known fixes to preserve: `saturationPortfolioOverview` uses `a.currentMarginalROI` (NOT `marginalROI`), reads `forecast.projectedSpendLevels` (NOT `forecast.forecast`), projected = last level's `marginalROI`, `projectedRisers` when `projected < currentMarginalROI`; `summaryPortfolioQuickView` reads healthScore/momentum from `summaryPerformanceSnapshot` (SummaryResult lacks them); triage budget pacing matches `t.includes("pacing") || t.includes("pace") || t.includes("overspend")` (`"pacing".includes("pace")` is false); route ordering: `/campaign-templates/launch-history` must precede `/campaign-templates/:templateId`.
- `CampaignTemplateService.TEMPLATES`: 6 templates — `black_friday`, `product_launch`, `cart_abandonment`, `brand_awareness`, `lead_generation`, `app_install` (minor: `app_install` config has `billing:` instead of `bidding:` — harmless).
- CommandCenterService and WeeklyMonthlyRoutinesService avoid circular deps — compose leaf services directly (realTimeMonitor, creativeOptimizer, saturationService, scorecardService, autonomousCampaignManager, budgetAutopilot).
- Budget autopilot allocation must always sum to monthlyBudget (cycle shifts cut from underperformers and re-adds to best platforms; percents recomputed).
- `CampaignAudienceExpansionService` uses `mem.find("audiences", ...)` — any audience builder should write to the same `audiences` collection.
- Creative shapes: `creatives` collection uses `{ _id, tenantId, name, type, status, headline, body, cta, tags, ... }` (per ExportService field list); `content_assets` uses `{ tenantId, title, type, status, tags, performance.prediction, ... }`.
- Wizard campaigns get `viaWizard: true` + `template` fields; duplicates get `duplicatedFrom`; mirrored get `mirroredPlatforms`; fatigue refresh writes `creativeRefreshState` on the campaign.

## Work State
### Completed
- **Round 1**: `batchUpdateStatus`, `batchUpdateBudget`, `getDailyOpsOverview`, `goalDashboard`, `goalQuickCheck` (GoalTracker); `scorecardDailySnapshot` (Scorecard); `portfolioRealTimeSummary` (RealTimeMonitor); `batchResolveAlerts` (AlertOrchestrator); `diagnosticsPriorityList` (PerformanceDiagnostics); `funnelPortfolioHealth` (ConversionFunnel); `healthPredictorQuickView` (HealthPredictor) — all plumbed + tested.
- **Round 2**: `budgetPortfolioOverview` (PortfolioBudgetOptimizer), `scoringLeadQuickView` (LeadScoring), `recommendationQuickView` (RecommendationEngine), `executeRecommendationBatch` (RecommendationEngine), `campaignSummaryQuickView` (CampaignSummary) — plumbed + tested.
- **Round 3 — 5 portfolio quick-views** (`PortfolioQuickViews.test.ts`, 5 tests): `creativePortfolioHealth`, `saturationPortfolioOverview`, `biddingPortfolioOverview`, `snapshotPortfolioSummary` (async), `summaryPortfolioQuickView`.
- **Round 4 — 5 batch actions** (`BatchActions.test.ts`, 6 tests): `biddingBatchApplyAdjustments(tenantId, priorityOnly=true)`, `snapshotBatchCapture(tenantId, name="Daily Ops Capture")` async, `saturationBatchMitigation`, `diagnosticsBatchFixPlan` (steps from `s.action/s.effort/s.expectedImpact/s.timeframe`), `creativeBatchRefreshPlan`.
- **Round 5 — daily execution** (`DailyExecutionDashboard.test.ts`, 3 tests): `goalBatchStatus` (uses `recommendGoalAdjustments`), `budgetRebalancePlan` (BudgetSimulator), `dailyExecutionDashboard(tenantId)` async aggregating 15 sub-calls into `sections`, `readyActions` (6 counts), `topActions`, `morningReport`, `healthVerdict`.
- **Round 6 — one-click execution layer** (`OneClickExecution.test.ts`, 15 tests): `CampaignAutoApproveService` (approval_settings, DEFAULT_SETTINGS, 7 methods, logs to approval_decisions), `CampaignTriageService` (6 alert types → actions, triage_logs), `CampaignTemplateService` (6 templates, launchTemplate, launched_templates), `CommandCenterService` (commandCenterSummary 4 cards, dailyBriefing, parseVoiceCommand 12 intents, quickActions 6). 19 delegations + 4 imports; ~20 routes; matching client methods.
- **Round 7 — set-and-forget layer** (`SetAndForget.test.ts`, 16 tests): `CampaignAudienceBuilderService` (buildAudience → `audiences`, syncAudienceToPlatforms, audienceQualityScoring, audienceLtvRanking, applyAudienceAutoActions → `audience_actions`, audienceSyncStatus); `BudgetAutopilotService` (enableAutopilot 4-platform allocation = monthlyBudget, autopilotStatus, runAutopilotCycle → `autopilot_log`, spendAlerts, autopilotDailySummary); `WeeklyMonthlyRoutinesService` (weeklyReview 6 sections, monthlyStrategyDeck 5 sections, aiOptimizationLog merges all 4 action logs). 14 delegations + 3 imports; 14 routes (audiences/… ×6, autopilot/… ×5, weekly-review, monthly-strategy-deck, ai-optimization-log); 14 client methods.
- **Round 8 — one-click execution part 2** (`OneClickExecutionPart2.test.ts`, 15 tests, all passing):
  - `CampaignLaunchWizardService.ts` — launchWizard (3-click launch: 5 steps all done, campaign inserted with viaWizard + template, logs to launched_templates), duplicateCampaign (paused copy with duplicatedFrom), mirrorCampaign (adds platforms + mirroredPlatforms), launchReadiness (3-item checklist).
  - `CreativeAutoRefreshService.ts` — generateCreative (description → N variants with headline/cta/predictedPerformance, stored in `creatives` via "ani"), detectFatigue (latest vs peak CTR, fatigued when drop >20%), runAutoRefresh (detect → generate 3/campaign → pending_approval + approval_decisions log), uploadAsset (auto-tags, brand safety, 4-platform resize, prediction, dedup vs `content_assets`), assetLibraryStatus.
  - `QuickFixService.ts` — 7 fixes (not_spending, creative_rejected, audience_too_small, tracking_broken, platform_api_error, budget_pacing_off, roas_drop), quickFixes (deterministic detections), applyQuickFix (logs to triage_logs as alertType "quick_fix"), fixAll.
  - `CrossModuleWorkflowService.ts` — campaignCreationWorkflow (8 automations: Tasks/Calendar/Docs/Sheets/Chat/CRM/Mail/Vault, logs to `workflow_log`), workflowLog.
  - Round 8 plumbing: 14 delegations + 4 imports; 14 routes (launch-wizard, campaigns/duplicate, campaigns/mirror, campaigns/:campaignId/launch-readiness, creatives/generate, creatives/fatigue, creatives/auto-refresh, assets/upload, assets/library-status, quick-fixes, quick-fixes/apply, quick-fixes/fix-all, campaigns/workflow, workflow-log); 14 client methods.
- **Round 9 — silent-guardian layer** (`SilentGuardian.test.ts`, 11 tests, all passing):
  - `BrandSafetyGuardianService.ts` — fraudProtectionStatus (6 capabilities, monitoredPlacements, protectedSpend), monitorPlacements (deterministic placement flags via hashStr: bot traffic %7, low viewability %11; statuses monitored/flagged/suspicious), autoPauseSuspicious (logs to `brand_safety_log` as auto_pause, pausedCount + protectedSpend = hashStr % 4000), crisisResponse (3 options: review / resume_safe / escalate), escalateToLegal (type escalation), resumeOnSafeInventory (alternativesFound ≥3), guardianLog (sorted desc).
  - `AttributionReportService.ts` — attributionReport (one-page: channels sorted revenue desc, plainLanguage explanations, topPaths with `→`, executiveSummary with ROAS, model `last_click_with_assisted_credit`, assisted credit = 28% of channel revenue), attributionQuery (intents: best_channel, channel_attribution, model_explanation via "first"/"linear"/"time", roas_summary, general; throws on empty query).
  - `CrossPlatformPerformanceService.ts` — crossPlatformPerformance (4 platforms sorted spend desc, roas, trend up/down/flat via hashStr, share %, totals; share sums to 100).
  - Round 9 plumbing: 10 delegations + 3 imports (brandSafetyGuardian, attributionReportService, crossPlatformPerformance); 10 routes — GET /fraud-protection-status, GET /placements/monitor, POST /placements/auto-pause, GET /brand-safety/crisis, POST /brand-safety/escalate, POST /brand-safety/resume, GET /guardian-log, GET /attribution-report, POST /attribution-query, GET /cross-platform-performance; 10 client methods.
- **Round 10 — frontend Command Center** (spec §1.2, first frontend round):
  - `src/pages/CommandCenter.tsx` at `/command-center` — morning dashboard: 4 summary cards (ROAS/budget pacing/alerts/AI suggestions), cross-platform performance bars (spend/roas/trend/share), morning report + health verdict + top actions, ready-actions grid (6 counts), budget autopilot allocation, audience sync status, silent-guardian status, attribution one-page (executive summary, top paths, plain-language metrics). Auto-refresh 30s toggle; per-call `.catch(() => null)`; unwraps `{ data }` envelope via `unwrap` helper. Registered in App.tsx route + Sidebar nav (after Dashboard) + CommandPalette.
  - Fixed pre-existing `client.ts` syntax error that blocked the ENTIRE frontend build: missing `campaignAttributionModeling: {` header (line ~2876) made `},` close the api object early → TS1005 cascade at 2894; esbuild "Unexpected (". Also fixed `launchWizard` param shadowing the `request` helper (renamed to `payload`, body key unchanged) and 5 invalid `request.post(...)` calls (→ `{ method: "POST", body: JSON.stringify(...) }`).
  - Fixed Round 7 bug: `audienceSyncStatus` crashed with 500 (`Cannot read properties of undefined (reading 'length')`) on `audiences` records lacking `platforms` (seeded by other services, e.g. CampaignAudienceExpansionService) — `(a.platforms || [])`. Added regression test in SetAndForget.test.ts (17 tests now).
  - Verified live: all 7 Command Center endpoints return data with a minted dev JWT (ROAS 3.84x, 77 ready actions, verdict "Needs attention" on seed data; cross-platform spend 0 on seed because metrics campaignIds don't match seed campaigns — cosmetic, not a bug).
- **Round 11 — help, shortcuts, mobile** (spec §13.2/§14.2/§11, final spec items; all frontend, no backend changes):
  - `src/components/HelpDialog.tsx` — contextual help: topbar `?` button + `?` key + `n0va:toggle-help` event. Tab 1 "Fix issues": fetches `api.adsMarketingModule.quickFixes()`, lists detected issues with detection + 1-click Apply per fix (applyQuickFix) + Fix All (fixAll), toasts feedback, links to /help, /templates, /campaigns/new, /approvals. Tab 2 "Shortcuts": §14.2 cheat sheet. Tab 3 "Voice": §14.3 command reference. Esc closes; links navigate then close.
  - `src/components/Layout.tsx` — §14.2 keyboard shortcuts (guard: plain-key shortcuts skipped while typing in INPUT/TEXTAREA/contentEditable; mod-shortcuts always active): `Ctrl/Cmd+N` → /campaigns/new, `Ctrl/Cmd+Shift+A` → /approvals, `Ctrl/Cmd+K` → palette (existing), `Ctrl/Cmd+/` → dispatch `n0va:toggle-assistant` (AIAssistant listens), `Shift+R` → dispatch `n0va:refresh-data` (CommandCenter listens → loadData), `G then D/C/R` → Dashboard/Campaigns/Reports (1.5s window via `pendingGo` state + timeout), `Esc` → close palette + help, `?` → toggle help. Sidebar wrapped in `hidden lg:block`; topbar responsive (`px-4 md:px-8`, search hidden on mobile, activity icon hidden on mobile, mobile "N0VA Ads" brand); content padding `p-4 md:p-8 pb-24 lg:pb-8` (bottom room for mobile nav).
  - `src/components/MobileNav.tsx` — §11 bottom nav (`lg:hidden`, fixed, safe-area padding): Home, Command, Campaigns, Audiences, Approvals.
  - `AIAssistant.tsx` — listens for `n0va:toggle-assistant` (opens panel, un-minimizes); FAB/panel repositioned `bottom-20 right-4 lg:bottom-6 lg:right-6`, panel `w-[calc(100vw-2rem)] max-w-[380px] h-[60vh] lg:h-[560px]` so it clears the mobile nav. `QuickActions.tsx` FAB likewise raised on mobile.
  - Verified: frontend tsc zero errors in all touched files (18 pre-existing errors unchanged); live backend check with cached dev JWT + `x-tenant-id` header — quick-fixes returned 7 fixes / 2 detected (tracking_broken, platform_api_error), applyQuickFix executed ("Re-sync pixel/CAPI", logged to triage_logs), fixAll applied 2.
- **Round 12 — build & suite GREEN**: added missing singleton exports `export const campaignIssueService = new CampaignIssueService();` (CampaignIssueService.ts) and `export const campaignOptimizerService = new CampaignOptimizerService();` (CampaignOptimizerService.ts) — the standard pattern every other service file follows (e.g. `campaignScorecardService` at CampaignScorecardService.ts:895). This fixed all 22 DeeperMarketingServices.test.ts failures (104/104 tests now pass) without touching the test file. Plus the frontend fixes listed under "Frontend tsc" above → `npm run build` green, full backend suite 2005/2005.
- **Round 14 — mobile viewport sweep + envelope-crash fixes** (all frontend, no backend changes): browse-verified ~30 pages at 375×812 via the gstack browse daemon (`goto <url>` + `eval page-check.js` pattern — see Temp\opencode\page-check.js which reads scrollWidth/clientWidth/mobileNav/overflowEls). Fixed "X is not a function" crashes from `{data: [...]}` envelopes in ~24 files (full list in Relevant Files) using `Array.isArray(r) ? r : r?.data || []`. Fixed backend-shape mismatches: Segmentation.tsx got `normSeg` normalizer (segments lack `groups`/`description`/`estimatedSize`); LandingPages.tsx got `normLp` normalizer (rows lack `views`/`conversions`/`tags`/`name`/`url`). Fixed 6 header rows that overflowed 375px by adding `flex-wrap justify-end` to the button containers (Audiences, Approvals stats grid `grid-cols-2 md:grid-cols-4`, Agents, Creatives, NotificationCenter, Dashboard, WebhooksPage). Debug technique that worked when the browse console dropped real error text: temporary `ErrBoundary` class in Layout.tsx writing `(window as any).__pageErr` in `componentDidCatch`, read via `eval` file — REMOVED after fixes verified. Final gates: frontend tsc ZERO errors, `npm run build` GREEN (9.4s), backend vitest 2005/2005 (22.8s).
- **Round 15 — N0VA MAIL core engine (first mail round; backend only, per user scope choice)**:
  - New mail collections: `mailboxes`, `mail_folders`, `mail_contacts`, `messages`, `mail_rules`, `mail_rules_log`. `MemoryStore.seed()` now seeds mail data for `tenant_001` (5 mailboxes incl. Ani Neural archive, 4 folders, 6 contacts, ~17 messages, 2 rules) — the seed builds an `insertedMailboxes` array because raw array records lack generated `_id`s. Full suite green afterwards (2042/2042), so seed counts are covered by existing tests.
  - 5 new services (each with singleton export): `MailboxService` (8 mailbox types, plan quotas free/pro/business/n0va1o, `safePct(used, quota)` floors tiny usage at 0.01% so percentUsed/alerts/status never zero out on large quotas; `mailboxQuota`, `storageAnalytics` w/ top senders+categories, alerts always ≥1 entry), `MailMessageService` (composeSend, saveDraft/sendDraft, receiveMessage auto-enrich+auto-rules, listMessages w/ folder/label/unread/starred filters, getMessage, getThread, markRead, toggleStar, move/archive/trash/restore/delete, apply/removeLabel, listFolders + custom folder CRUD, unreadSummary), `MailRulesService` (visual rules w/ conditions+actions; 6 templates: newsletters/invoices/high_priority/social/vendor/meeting_reminders; App-Script-style DSL for script rules: `if <field> [contains|is|is_not] "val" then <action> ["target"]` with label/archive/move/mark_read/forward/star/notify/auto_reply; evaluateRule/evaluateAllRules/testRule/runScriptRule; rulesDashboard + log), `MailSearchService` (scored search, semanticQuery intents: unread_emails/from_sender/high_priority/with_attachments/about_topic/category/recent/yesterday, searchSuggestions from contacts+senders, searchStats), `MailAIService` (enrichMessage: deterministic priority/sentiment/category/summary/actionItems/spamScore via hashStr; smartReply 3 tones w/ confidence; summarizeThread w/ decisions/openQuestions/nextSteps; meetingPrep w/ agenda/times/prepNotes; emailIntelligence digest).
  - Plumbing: 45 `mail*` delegations + 5 imports in AdsMarketingModuleService.ts; 45+ `/mail/*` routes in ads-marketing-module.ts (POST `/mail/rules/test` placed BEFORE `:ruleId` param routes; `mail/mailboxes/:mailboxId` distinct from `mail-folders/:folderId`); ~45 client methods in client.ts (`adsMarketingModule.mail*`, URLSearchParams for filters, DELETE with `encodeURIComponent(label)`).
  - `NovaMailRound1.test.ts`: 37 tests all passing. Test-tenant fixtures include a `mail_contacts` row (search-suggestions) and a direct `mem.insert` for the script-rule message (receiveMessage would auto-run rules and double-apply). Script rules (`kind:"script"`) skip conditions/actions validation — only `script` body required.
  - Gates: full suite 2042/2042 green across 3 consecutive runs (91 files, ~13-20s); frontend `npx tsc --noEmit` zero errors; `npm run build` GREEN (~8.2s).
  - Live smoke test (dev server on :4000 restarted to load new routes; token at `%TEMP%\opencode\n0va_token.txt`, header `x-tenant-id: tenant_001`): storage-analytics 5 mailboxes/17 msgs/2.83 MB used; ai/intelligence 10 unread; inbox list 13 total/10 unread; semantic "unread emails with attachments" → intent with_attachments, 3 results.
- New test files: `PortfolioQuickViews.test.ts` (5), `BatchActions.test.ts` (6), `DailyExecutionDashboard.test.ts` (3), `OneClickExecution.test.ts` (15), `SetAndForget.test.ts` (17), `OneClickExecutionPart2.test.ts` (15), `SilentGuardian.test.ts` (11), `NovaMailRound1.test.ts` (37), `NovaMailRound2.test.ts` (38) — all passing.
- **Round 16 — N0VA MAIL frontend pages** (5 new pages, all frontend):
  - `src/pages/MailInbox.tsx` at `/mail` — folder nav from `mailUnreadSummary` (horizontal chip row on mobile, left column on lg), message list from `mailMessages({folder, limit:100})` with unread dot/star/priority/attachment indicators, thread bottom-sheet on mobile (`fixed z-50 max-h-[85vh]`, static right-column equivalent on lg) via `mailThread`, action bar (Reply→prefilled compose, mark read, star, archive/trash/restore/delete, Move to… select), label add/remove (`mailApplyLabel`/`mailRemoveLabel`), compose modal (`mailSend` + `mailSaveDraft`, mailbox + importance selects). Note: thread backdrop covers header on mobile — close thread before Compose (by design).
  - `src/pages/MailSearch.tsx` at `/mail/search` — semantic query box (`mailSemanticSearch`) with intent chip + explanation, keyword search (`mailSearch` w/ folder/priority/unread/attachment filters), live suggestions (`mailSearchSuggestions`), stats cards (`mailSearchStats`).
  - `src/pages/MailRules.tsx` at `/mail/rules` — `mailRulesDashboard` (stats cards, rules list w/ visual conditions+actions chips or script `pre` preview, toggle switch, test modal, delete), 6 templates via `mailRuleTemplates` + `mailInstantiateRuleTemplate`, create modal for visual (condition/action row builders) and script (DSL textarea) rules via `mailCreateRule`, test modal via `mailTestRule` (sample subject/from/body → wouldMatch/failedConditions/actionsToRun). Script rules skip conditions/actions validation — payload only needs `script`.
  - `src/pages/Mailboxes.tsx` at `/mail/mailboxes` — `mailStorageAnalytics` cards (mailboxes/storage/messages/critical) + quota alerts, `mailMailboxes` list w/ quota bars + enable/disable, create mailbox modal (`mailCreateMailbox`), top senders + by-category.
  - `src/pages/MailAI.tsx` at `/mail/ai` — `mailIntelligence` cards + attention items + unread-by-priority bars + priority digest; per-digest expand: `mailEnrich` (priority/sentiment/category/action items/spam) + `mailSmartReply` (tone chips w/ confidence + copy); thread analyzer (`mailSummarizeThread` + `mailMeetingPrep`) with decisions/openQuestions/nextSteps + agenda/prep notes.
  - Registration: 5 lazy routes in App.tsx (before the catch-all), 5 navItems in Sidebar.tsx (Mail icon was already imported; added Mailbox/Zap/Sparkles/Search were already present — watch TS2300 duplicate-import errors: Zap and Sparkles were ALREADY imported in Sidebar), 5 PAGE_ITEMS in CommandPalette.tsx (added Mail/Mailbox/Inbox icons to its big lucide import). MobileNav untouched (5-slot limit).
  - Unwrap convention applied everywhere: object endpoints `r?.data ?? r`, bare-array endpoints `Array.isArray(r) ? r : r?.data || []` (mailboxes, folders, templates). All pages also listen for `n0va:refresh-data` + have Refresh buttons.
  - Gates: frontend tsc ZERO errors, `npm run build` GREEN (10.8s). Browse-verified at 375×812 via gstack browse daemon (`%TEMP%\opencode\page-check.js`): all 5 pages zero horizontal overflow, zero console errors (only pre-existing React Router/socket.io warnings); interactions tested live — thread open/close on mobile, compose modal, semantic search ("unread emails with attachments" → intent with_attachments, 2 results), rules test modal (filled sample → correct "Would NOT match" for newsletter rule vs invoice subject). Browse daemon tip: after a tab crash `storage set` may be lost — re-set token, and use `fill "input[placeholder*='...']"` selectors + `click "@eN"` with fresh snapshots; a click that navigates resets refs.
- **Round 17 — contacts, N0VA1O agent, compliance, voice** (backend + frontend; `NovaMailRound2.test.ts`, 38 tests green):
  - `MailContactService.ts` — contacts CRUD, contactGroups, contactDashboard (summary/categories/topContacts), mostContacted (ranked by message count), getContactProfile (messages + engagement), auto-sync contacts from message senders, saveContactFromEmail.
  - `MailAgentService.ts` — N0VA1O agent: agentStatus, runAgentCycle (OOO auto-reply flags `ooo_replied` on unread inbox msgs; due scheduled emails composed via `mailMessage.composeSend`), setOutOfOffice (auto-reply template), listScheduled/ scheduleSend/cancelScheduled (schedules in `mail_schedules`), extractTasksFromEmail (regex `- [ ]` / `[ ]` checklist → `mail_tasks`), listTasks/completeTask, agentLog (sorted desc).
  - `MailComplianceService.ts` — complianceSummary, listPolicies/setRetentionPolicy/deletePolicy/applyRetentionPolicy (sweep skips messages matching any active non-expired hold by subject/from lowercase includes; policy validation: folder exists + days parse > 0), holds (placeHold by subject/from regex, releaseHold by `_id`, holdStatus), auditLog (audit_trail), scanPii (regex email/Ssn/phone/creditCard/iban, per-message counts; phone accepts optional `\s*` after area code). New collections: `mail_schedules`, `mail_tasks`, `mail_holds`, `audit_trail` (+ `pii_findings` optional).
  - `MailVoiceService.ts` — parseVoiceCommand (13 commands; send/schedule patterns use `i` flag on ORIGINAL case-preserved text, subject case preserved) + executeVoiceCommand (send email to X about Y, schedule email to X at <ISO>, read my unread emails, search for X, mark (all|the last) email read, star the last email, turn on/off out of office, summarize the latest thread, unread count, create rule from <template>) — named recipients resolved via `mail_contacts`; getMailbox falls back to first active mailbox when mailboxId omitted.
  - Plumbing: 4 imports + ~40 `mail*` delegations in AdsMarketingModuleService.ts; ~35 routes in ads-marketing-module.ts (`/mail/contacts*`, `/mail/agent/*`, `/mail/compliance/*`, `/mail/voice/*`); ~30 client methods in client.ts.
  - Frontend: `MailContacts.tsx` (/mail/contacts — dashboard cards, groups, contact list w/ profile drawer, add/edit modal), `MailAgent.tsx` (/mail/agent — status cards, OOO toggle + auto-reply template, schedule list + create, task list + extract-from-thread + complete), `MailCompliance.tsx` (/mail/compliance — summary cards, retention policies CRUD + apply, holds place/release, audit log, PII scan). Registered lazy routes in App.tsx, nav items in Sidebar.tsx, palette items in CommandPalette.tsx.
  - Inbox polish: `MailInbox.tsx` Esc closes thread sheet + Ctrl/Cmd+M opens compose (deps `[mailboxes]`); `Sidebar.tsx` fetches `mailUnreadSummary()` → unread-count badge on /mail nav item (desktop; listens for `n0va:refresh-data`).
  - Gates: full backend suite 2080/2080 (92 files); backend tsc — no NEW errors (227 pre-existing unchanged); frontend tsc ZERO errors; `npm run build` GREEN (~42.7s); live smoke: contacts dashboard 6 contacts, agent status 0 OOO/0 scheduled/0 tasks, compliance summary 0/0/0, voice help 13 commands, voice execute "send email to john.smith@partner.com about Round 17 test" → intent send_email, PII scan 18 messages 0 findings. Browse-verified at 375×812: /mail/contacts, /mail/agent, /mail/compliance zero horizontal overflow; mobile bottom nav visible; Esc closes thread sheet; Ctrl+M opens compose; sidebar Mail badge shows unread count (10).
  - Backend restart note: dev server must be restarted after backend changes to load new routes (no watch). Reliable restart = `Start-Process cmd /c` a launcher .cmd that does `cd backend && hermes-node tsx/dist/cli.mjs src/index.ts > log 2>&1` (Start-Process with inline redirect args mangles `--require/--import` quoting → MODULE_NOT_FOUND); the running node cmdline is `node.exe --require <n0va\node_modules\tsx\dist\preflight.cjs> --import file:///...loader.mjs src/index.ts`. NOTE: if the server dies with a bare `^C` in the log mid-session, restart with `-WindowStyle Hidden` (the cmd window can receive console kill signals from the parent shell otherwise).
- **Round 18 — templates, signatures, spam, follow-ups, attachments, analytics** (backend + frontend; `NovaMailRound3.test.ts`, 46 tests green):
  - `MailTemplateService.ts` (`mailTemplates`) — template CRUD + render (`{{var}}` regex extraction, missing-var reporting), sendFromTemplate, sendBulk (string recipients or `{to, variables}` objects, per-recipient failure tolerance), templateStats, templateUsageLog. Collections: `mail_templates`, `mail_template_usage`.
  - `MailSignatureService.ts` (`mailSignature`) — per-mailbox signatures stored on `mailboxes.signatureConfig` (`text/enabled/isDefault/updatedAt`), listSignatures, update/toggle/default, composePreview (appends sig to body), signaturesDashboard.
  - `MailSpamService.ts` (`mailSpam`) — deterministic spam scoring via `hashStr` seed (% 41 base + keyword `+12`/unknown domain `+15`/link-heavy `+10`/reputation `+8`, cap 100, ≥60 = spam), scanMessage/scanAll (moves flagged inbox msgs to `folder:"spam"` + `flags:["spam"]`), quarantine list, reportSpam (blocks non-contact senders), reportNotSpam (restore + allow), blockedSenders/allowedSenders CRUD, spamLog. Collections: `mail_blocked_senders`, `mail_allowed_senders`, `mail_spam_log`.
  - `MailFollowUpService.ts` (`mailFollowUp`) — snooze/unsnooze (message `snoozedUntil` + `snoozed: true`; future ISO required), listSnoozed (with `snoozedOverdue`), markAwaitingResponse (`awaitingResponse` + `responseDeadline`), markResponded (clears flags, closes open follow-ups `status:"done"` + `completedAt`), createFollowUp/listFollowUps/completeFollowUp/deleteFollowUp, followUpSummary, suggestions. Collection: `mail_follow_ups`.
  - `MailAnalyticsService.ts` (`mailAnalytics`) — overview, volumeTrend (14-day), categoryMix, topSenders, responseTimeStats (only threads with a reply), busiestHours, activityByFolder, mailboxStats, executiveSummary. Uses `estimateMessageBytes` from MailboxService.
  - `MailAttachmentService.ts` (`mailAttachment`) — listAttachments (from message.attachments, `att_${hashStr(messageId+fileName)}` ids), getAttachment, scanAttachment (deterministic seed%5 suspicious / seed%9 DLP, persists onto `attachments[].scan` with scannedAt), quarantineAttachment, attachmentStats.
  - Plumbing: 6 imports + ~75 `mail*` delegations in AdsMarketingModuleService.ts; ~70 routes in ads-marketing-module.ts (`/mail/templates*`, `/mail/signatures/:mailboxId`, `/mail/spam/*`, `/mail/messages/:messageId/snooze|unsnooze|awaiting|responded`, `/mail/followups*`, `/mail/analytics*`, `/mail/attachments*`); ~58 client methods in client.ts. IMPORTANT: `mailSignaturePreview` client method was added in Round 18 for POST `/mail/signatures/:mailboxId/preview` (route existed, client didn't).
  - Frontend pages: `MailTemplates.tsx` (/mail/templates — dashboard cards, template cards w/ variable chips, edit/create modal, single-send + bulk-send modals, recent sends), `MailSignatures.tsx` (/mail/signatures — cards, per-mailbox signature cards w/ toggle/edit/preview + compose preview block), `MailSpam.tsx` (/mail/spam — cards, quarantine w/ scan/not-spam/report, blocked + allowed lists, activity log), `MailFollowUps.tsx` (/mail/follow-ups — cards, smart suggestions, open/completed follow-ups, snoozed list, create modal), `MailAnalytics.tsx` (/mail/analytics — executive summary, volume bars, category mix, top senders, response times, busiest hours, folders, mailboxes table), `MailFiles.tsx` (/mail/files — cards, type/status filters, file list w/ scan badge + view/scan/quarantine, detail modal). Registered in App.tsx + Sidebar.tsx + CommandPalette.tsx.
  - Inbox polish: `MailInbox.tsx` now has Snooze… select in the thread action bar (Later today / Tomorrow / Next week presets via `snoozePresets()`) + violet "Snoozed until …" chip with unsnooze X in the label row.
  - Gates: full backend suite 2126/2126 (93 files); `NovaMailRound3.test.ts` 46/46 (7 template, 6 signature, 10 spam, 8 follow-up, 9 analytics, 6 attachment); backend tsc — no NEW errors; frontend tsc ZERO errors; `npm run build` GREEN (45.8s). Live smoke: template create/render/send/bulk (2 sent 0 failed), signature update + preview, spam scan-all (13 scanned), attachment scan (clean). Browse-verified at 375×812: all 6 pages zero horizontal overflow, real data renders (files page: 4 attachments w/ statuses; analytics: full exec summary); console only pre-existing React Router/socket.io warnings. NOTE: in-memory data resets on backend restart — the smoke-created template vanished after restart (expected).
  - Browse daemon tip (Round 18): on Windows the daemon dies when the launching PowerShell exits — run `Start-Process <browse.exe> daemon -WindowStyle Hidden` once (with `BROWSE_SERVER_SCRIPT` env set to `dist/server-node.mjs`), then `browse status` to confirm healthy before using `goto`/`eval`; set the n0va token via `storage set` while ON a localhost:5173 origin (localStorage throws SecurityError on about:blank). Round 19 addenda: `browse eval` only accepts FILE paths (inline JS errors "File not found"), `browse viewport 375x812` switches the viewport, and BOTH the client shell AND the daemon need `BROWSE_SERVER_SCRIPT` (start the daemon via `cmd /c "set BROWSE_SERVER_SCRIPT=dist/server-node.mjs && \"<browse.exe>\" daemon"` -WindowStyle Hidden to guarantee propagation).
- **Round 19 — real-time collaboration, predictive intelligence, bulk campaign agent, eDiscovery, custom domains** (backend + frontend; `NovaMailRound4.test.ts`, 52 tests green, stable across 8 consecutive runs):
  - `MailCollaborationService.ts` (`mailCollab`) — comments (mentions auto-resolved from `mail_contacts` by name/email substring), per-message/thread comment lists, deleteComment, reactions (dedupe per user+emoji, grouped counts), shared drafts (collaborators contactId|null, statuses draft/in_review/approved, dueAt), presence (online/away/busy + viewingSubject + typing), collaborationState, collaborationSummary. Collections: `mail_comments`, `mail_reactions`, `mail_shared_drafts`. sharedDrafts list maps `{ draftId: d._id, ...d }` — pages must use `draftId`.
  - `MailPredictiveService.ts` (`mailPredict`) — responseTimePrediction (observed reply latency vs sender model), outcomePrediction (intents meeting_request/approval_needed/invoice/contract/escalation/newsletter/info_only), churnRisk (>21d high, >10d medium, >5d unanswered medium), optimalSendTime (24h histogram), relationshipHealth (score/level: strong ≥75, healthy ≥50, cooling ≥30), intentPrediction (6-intent matrix), nudgeSuggestions (filter `m.read === false` — NOT `unread`), workloadForecast, sendTimeSuggestion, predictiveDashboard.
  - `MailCampaignService.ts` (`mailCampaign`) — create/list/delete (guards active + pending_approval), launch (audience all|groups(tags)|query|contacts; >50 recipients → `pending_approval` HITL gate), execute (deterministic via stable seed — see flake fix in Important Details; auto-pause when complaint rate >2%; rate limit 200/h default; uses `mailTemplates.renderTemplate` + `mailMessage.composeSend`), approve/reject/pause/resume, campaignStats (rates + recentEvents + A/B winner), campaignsDashboard, campaignResponseHandling (interested/question/not_interested/other + suggestions), campaignLog. Collections: `mail_campaigns`, `mail_campaign_events`, `mail_campaign_log`. **listCampaigns/getCampaignPublic rows map `campaignId: c._id` (pages must NOT rely on raw `_id`)**. Private `getCampaign` → public `getCampaignPublic`.
  - `MailDiscoveryService.ts` (`mailDiscovery`) — scopeSearch (scope keys: query/from/folder/label/dateFrom/dateTo/hasAttachments/attachmentType/unreadOnly), saved searches CRUD+run, createExport (csv/eml/mbox/pdf + Bates `BATES-0001…` + optional `redactPii`), exports/getExport/deleteExport (export payload exposes `download.filename`/`download.content`), discoverySummary. Collections: `mail_saved_searches`, `mail_exports`.
  - `MailDomainService.ts` (`mailDomain`) — registerDomain (domain regex validation, 6 DNS records: mx/spf/dkim[selector `n0va`]/dmarc/mtasts/tlsrpt; free plan dmarc `p=none`, business `p=quarantine`), list/get/delete (rows map `domainId: d._id`), verifyDomain (status `action_required` unless verifiedCount === 6), domainHealth (uptime/deliverability/reputation/blacklist/certStatus), reputationMonitor (+alerts), setDomainPolicy (plan gates: dmarcEnforce/mtaSts require business+, brandProtection requires enterprise or `n0va1o`; dmarcEnforce rewrites record to `p=reject`), domainLog, domainSummary. Collections: `mail_domains`, `mail_domain_log`.
  - Plumbing: 5 imports + ~90 `mail*` delegations at the END of AdsMarketingModuleService.ts (after the mailAttachment block); ~60 routes before `export default router` in ads-marketing-module.ts (`/mail/collab/*`, `/mail/predict/*`, `/mail/campaigns/*`, `/mail/discovery/*`, `/mail/domains/*`); ~70 client methods at the END of client.ts (after `mailQuarantineAttachment`).
  - Frontend pages: `MailCollaboration.tsx` (/mail/collaboration — presence, thread comment picker, reaction bar, shared draft cards + create/advance), `MailPredict.tsx` (/mail/predict — dashboard, send timing, churn + nudges, outcome/intent analyzer, relationship health), `MailCampaigns.tsx` (/mail/campaigns — stats cards, campaign list w/ launch/approve/reject/pause/resume/delete, stats modal, create modal w/ audience modes + A/B subject, log), `MailDiscovery.tsx` (/mail/discovery — scope search + results, saved searches, exports + download, modals), `MailDomains.tsx` (/mail/domains — reputation monitor, DNS tables w/ verify, policies modal, register modal, log). Registered in App.tsx + Sidebar.tsx + CommandPalette.tsx (all 5 nav items confirmed rendering in browse).
  - Gates: full backend suite 2178/2178 (94 files); `NovaMailRound4.test.ts` 52/52 stable 8×; backend tsc — no NEW errors in new files; frontend tsc ZERO errors; `npm run build` GREEN (~12s). Live smoke (restarted backend): collab comment+reaction+summary, predict dashboard, template→campaign create→launch (6 sent 0 failed), discovery export w/ Bates + PII redaction, domain register (4/6 verified) + verify + policies — all OK. Browse-verified at 375×812: all 5 pages zero horizontal overflow, zero errors, mobile bottom nav present (page-check's `mobileNavVisible` reads the FIRST `nav` — the hidden sidebar — and can report false; check fixed `NAV.lg:hidden` elements directly).
- **Round 20 — voice & multimodal messaging (§3.9), AI-assisted rule generation, batch message operations, Neural Mailbox (§3.2.2)** (backend + frontend; `NovaMailRound5.test.ts`, 48 tests green, stable across 5 consecutive runs):
  - `MailVoiceNoteService.ts` (`mailVoiceNote`) — createVoiceNote (messageId required, durationSec clamped ≥1, deterministic pipeline: 1-3 speakers w/ name `Speaker A…`, emotion from `EMOTIONS` const, confidence 78-97, action items from keyword list, summary; `generateTranscript: false` → "transcript not generated" summary), list/get/delete, voiceNoteDashboard (emotionMix, topSpeakers, actionItems). Collections: `mail_voice_notes`, `mail_voice_log`. Determinism seed: `${msg.from.email}|${msg.subject}|${audioData}` — do NOT feed genId ids into hashes.
  - `MailMultimodalService.ts` (`mailMultimodal`) — attachVideo (3-5 chapters `3 + hash%3`, auto-summary `X chapters`), attachScreenRecording (compressionPct 40-94, OCR text, UI elements, workflow steps), attachCodeSnippet (language via `LANG_KEYWORDS` — TypeScript/SQL/JavaScript…; vulnerabilities via `VULN_PATTERNS` w/ hash-seeded severity low/medium/high), createPoll (≥2 options)/votePoll (voter dedupe + closed throw + index range check)/pollResults (pct rounded, sentiment positive/neutral/negative, trend rising/stable, pcts sum 100)/closePoll, list/get/delete content blocks, multimodalDashboard. Collections: `mail_content_blocks`, `mail_polls`.
  - `MailNeuralService.ts` (`mailNeural`) — neuralOverview (auto-priority: `hashStr(email|subject+"prio")%100` + importance boost ±22/18 + urgent-keyword +15, capped 1-99, sorted desc), smartDraftSuggestions (4 REPLY_TEMPLATES + tone concise/warm), taskExtraction (parses `[ ]`/`[x]` checklist lines via `^\s*[-*]?\s*\[(?: |x|X)\]\s*(.+)$`), predictiveUnsubscribe (sender ≥3 inbox msgs + category in `DECLINING_SIGNALS` [newsletter/promotion/social] + `hashStr(email|category+"eng")%100 < 40`), smartArchive (read + importance≠high + !starred + olderThanDays, `apply` flag persists), conversationHealth (stale >7d/awaiting >3d, critical >14d), escalationQueue (unread high importance + urgent keywords, urgencyScore 70-99), learningLoop (accept/reject only, item.subject required), neuralMailboxDashboard. Collection: `mail_neural_log`. **Test fixture note: only `promo@shop.xyz` eng=39 flags (<40); other senders eng 67/87/67/98 — 5-sender fixtures produce exactly 1 offer.**
  - Extended `MailRulesService.ts` (`aiGenerateRule` + private `aiTestRule`): keyword/pattern parsing (from-address regex, quoted subject `"…"`, category regex, has_attachment, high importance; actions archive/mark_read/move/label/forward/star/notify/move-to-trash; label regex lowercases target), reuses `createRule`; `testBeforeEnable` → created paused + dry run over ≤20 recent messages returning `{tested, scanned, wouldMatch, samples}`; return `{ruleId, conditions, actions, enabled, test, summary}`. **tsc: `test` var must be explicitly typed (union of two branches doesn't narrow on `tested`) — keep the annotated type.**
  - Extended `MailMessageService.ts` (`batchOps`): 11 ops (move/archive/trash/restore/delete/star/unstar/mark_read/mark_unread/apply_label/remove_label), throws on unknown op/empty ids, `skipped` counts not-found ids, move needs `opts.destination`… wait: move uses `opts.destination`-style per implementation — summary strings like `2 message(s) move to "archive"`, `1 skipped`. Labels need `opts.label`.
  - Plumbing: 3 imports + ~56 tail `mail*` delegations in AdsMarketingModuleService.ts (`mailBatchOps` + `mailAiGenerateRule` in the message/rules sections, voice/multimodal/neural at the END); ~31 routes before `export default router` (`/mail/messages/batch`, `/mail/rules/ai-generate`, `/mail/voice-notes*` [dashboard route BEFORE `:noteId`], `/mail/multimodal/*`, `/mail/polls*`, `/mail/neural/*` — neural overview/dashboard take `req.query.mailboxId` with `typeof === "string"` guard for tsc); ~30 client methods after `mailDomainSummary` in client.ts.
  - Frontend: `MailVoice.tsx` (/mail/voice — stat cards merge voice-note + multimodal dashboards, record form, video/recording/code-snippet forms, poll create/vote/results/close, expandable voice notes w/ transcript+speakers+action items, content blocks w/ per-type detail, polls list) registered in App.tsx + Sidebar (Mic icon) + CommandPalette; `MailRules.tsx` got an "AI rule" modal (textarea + dry-run checkbox → `mailAiGenerateRule`, conditions/actions chips, dry-run counts, Enable/Pause toggle via `mailToggleRule`); `MailAI.tsx` got a "N0VA1O neural mailbox" card (attention counters, auto-priority queue w/ Accept/Reject learning buttons, smart drafts, extracted tasks + Smart archive button, unsubscribe intelligence, escalations, recent learning log).
  - Gates: full backend suite 2226/2226 (95 files); `NovaMailRound5.test.ts` 48/48 stable 5×; backend tsc — NO new errors (fixed 2 route TS2345 query guards + MailRulesService test union type); frontend tsc ZERO errors; `npm run build` GREEN (~7s). Live smoke (restarted backend): voice note create (anger/3 speakers/86%), AI rule generate (dry run 17 msgs), batch star, poll create+vote, video attach (5 chapters), recording (70% compressed, 4 steps), snippet (1 vuln), neural learning accept, smart archive, blocks list — all OK. Browse-verified at 375×812: /mail/voice zero overflow zero errors (voice note + 3 blocks + poll render, stat cards correct after merging multimodal dashboard), /mail/rules AI modal opens + generates via UI, /mail/ai neural section renders live (auto-priority scores, Accept/Reject, learning log). OverflowEls flagged on /mail/ai are `truncate`-class spans (content-width > clip-width by design — same pattern as pre-existing digest list), benign.
- **Round 21 — Mail Command Center: daily execution with one-click actions** (backend + frontend; `NovaMailRound6.test.ts`, 23 tests green, stable across 3 consecutive runs):
  - `MailCommandCenterService.ts` (`mailCommand`) — `mailCommandCenter(tenantId)` (4 cards: unread/priority/followUpsDue/scheduledToday; 6 sections: priorityQueue/followUps/scheduled/escalations/healthAlerts/campaignApprovals; counts: openTasks/quarantine/archiveCandidates/storageCritical/domainsFlagged/healthCritical; storage + domains objects; readyActions; attentionScore = overdue + escalations.total + storageCritical + healthCritical + pendingApproval; verdict: 0 "Good to go" / ≤3 "Mostly clear" / else "Needs attention"; 6-line morningReport; seed via hashStr) + one-click delegates: completeFollowUp, sendScheduledNow, approveCampaign, rejectCampaign, runRulesSweep, rescanSpam, runAgentCycle, smartArchiveNow.
  - Extended `MailRulesService.ts` (`sweepRules`): scans inbox, per-message per-rule eval — **script rules evaluated individually (`runScriptRule`) because `runAllScriptRules` returns no results array**; visual via `evaluateRule`; match = `r.matched || (applied.length > 0)`; `rulesChecked` counts ONLY enabled rules; returns `{messagesScanned, rulesChecked, matchedMessages, actionsApplied, results (first 10), summary}`.
  - Extended `MailAgentService.ts` (`sendScheduleNow`): composeSend + status "sent" + sentAt; no-op for already-sent/cancelled; throws on unknown id; logs `schedule_sent`.
  - Plumbing: 1 import + 11 delegations in AdsMarketingModuleService.ts; 11 routes before `export default router` (`GET /mail/command-center`, `POST /mail/rules/sweep`, `POST /mail/schedules/:scheduleId/send-now`, `POST /mail/command-center/follow-ups/:followUpId/complete`, `POST /mail/command-center/schedules/:scheduleId/send-now`, `POST /mail/command-center/campaigns/:campaignId/approve|reject`, `POST /mail/command-center/rules-sweep|spam-rescan|agent-cycle|smart-archive` — agent-cycle takes `req.query.mailboxId` with `typeof === "string"` guard); 11 client methods after `mailNeuralDashboard` (`mailSweepRules`, `mailSendScheduleNow`, `mailCommandCenter`, `mailCommandCompleteFollowUp`, `mailCommandSendScheduledNow`, `mailCommandApproveCampaign`, `mailCommandRejectCampaign`, `mailCommandRunRulesSweep`, `mailCommandRescanSpam`, `mailCommandRunAgentCycle`, `mailCommandSmartArchive`).
  - Frontend: `MailCommandCenter.tsx` (/mail/command-center — verdict card + attention score + Refresh all, 6-line morning report, 4 stat cards, one-click action bar (Run rules / Rescan spam / Agent cycle / Smart archive w/ candidate count), ready-actions footer, 6-section grid (priority queue, follow-ups due w/ Done, scheduled sends w/ Send now, campaign approvals Approve/Reject, escalations + health alerts, housekeeping), 30s auto-refresh toggle, `n0va:refresh-data` listener, skeleton + error card, busy-per-action). Registered in App.tsx (after /mail/voice) + Sidebar ("Mail Command", LayoutDashboard, after Voice & Media) + CommandPalette.
  - Inbox polish: `MailInbox.tsx` batch selection mode (select-all checkbox vs `messages.length`, `selected` array keyed on `m._id`, toolbar: Read/Unread/Star/Unstar/Archive/Trash/Delete-in-trash/Label-apply/Clear via `mailBatchOps`); `MailRules.tsx` Run-now button (Play icon, `hidden sm:inline` label, toast w/ `res.summary`); `Sidebar.tsx` mail badge now also refreshes on route change (`useLocation` dep).
  - Gates: full backend suite 2249/2249 (96 files); `NovaMailRound6.test.ts` 23/23 stable 3× (fixes during dev: `rulesChecked` 3→2 paused-rule exclusion, `matchedMessages` 1→2 script-rule counting, attentionScore 4→5); backend tsc — NO new errors (227 pre-existing baseline); frontend tsc ZERO errors (added `Play` to MailRules lucide import); `npm run build` GREEN (~6.5s). Live smoke (restarted backend via be17.cmd): command-center cards/verdict/sections, rules sweep (13 msgs/2 rules/1 matched), spam rescan (13 scanned), agent cycle (idle), schedule create → send-now ("Round 21 smoke" sent), smart archive, batch star via `_id` — all OK (NOTE: `mail/messages` list rows key on `_id` — `messageId` is the RFC header and batch skips it; body key is `messageIds` + `Content-Type: application/json`). Browse-verified at 375×812: /mail/command-center zero overflow zero errors (mobile bottom nav present, verdict "Good to go", cards UNREAD 10/PRIORITY 5, Run rules click → "Swept…" toast), /mail batch select-all → Star → toast, /mail/rules Run-now (icon-only at 375px, title attr) → sweep toast. OverflowEls on /mail/command-center are `truncate`-class paragraphs (benign, same as /mail/ai).
- **Round 22 — Mail Ops & Incident Response (§8.1/§8.2) + Storage Tiering & AI Cleanup (§3.3.1/§3.3.2)** (backend + frontend; `NovaMailRound7.test.ts`, 29 tests green, stable across 4 consecutive runs):
  - `MailOpsService.ts` (`mailOps`) — SEVERITIES P0-P4 + RESPONSE_PLANS + escalation ladder (P4→P3…P0→P0). opsDashboard (health smtpUptime/imapUptime as "99.9x%" strings, queueDepth from scheduled.pending + open incidents, queueVerdict normal/≤4 elevated/critical, errorRate; performance avgDeliveryMs/p99SearchMs/attachmentProcAvg/aiLatencyMs; security spamCaught/falsePositives/malwareBlocked/authAnomalies; business activeMailboxes/messagesToday (uses `unread.totals.totalMessages` NOT `.messages`)/storageUsedBytes/aiQueriesToday; alerts always ≥1 (2 on seed); summary; seed), incidents (sorted desc; `{incidents,total,open}`), createIncident (P0-P4 + title required; responsePlan; throws "severity must be one of P0-P4"/"title is required"), acknowledgeIncident (no-op w/ "already resolved" when resolved), escalateIncident (ladder; stays open w/ escalatedTo), resolveIncident (returns incident WITH resolvedAt stamped — must re-read after update, stale row lacks it), incidentLog (categories `incident_*`), retryFailedQueue (deliveryStatus "failed" or flags `delivery_failed` → sent; summary "clean" when 0), takeCheckpoint (`cp_${Date.now()}_${random6}`; counts mailboxes/messages/contacts/domains/schedules/rules; label optional), deployThreatRule (deterministic 1-3 rules + 40-99 signatures + scanAll rescan; shape `{rulesDeployed, signaturesUpdated, newlyCaught, rescan:{scanned,moved}, summary}`), runHousekeeping (composes sweepRules + scanAll + smartArchive apply:true + runTiering + runAgentCycle; `stages` block + summary "Housekeeping done"), opsLog (categories housekeeping/tiering/threat_rule/retry/checkpoint/incident_*; shape `{entries:[{category,detail,at,...}]}`).
  - `MailStorageService.ts` (`mailStorage`) — TIER_NAMES hot/warm/cool/cold, DEFAULT_POLICY `{autoTier:false,coolAfterDays:30,coldAfterDays:90}` stored in `mail_storage_policy` (auto-inserted via private `policy()`), tierFor by age (<7 hot, <30 warm, <90 cool, else cold; policy thresholds). tieringStatus (per-tier counts/bytes via `estimateMessageBytes` imported from MailboxService + quota + policy + summary), setTieringPolicy (validates coldAfterDays > coolAfterDays; throws "coldAfterDays must be greater than coolAfterDays" — route surfaces as 500, app-wide convention), runTiering (only downgrades to cool/cold; sets `storageTier` + `tieredAt`; idempotent "All messages already on the correct tier"), cleanupSuggestions (newsletters: `(m.ai?.category || m.category || "")` === newsletter + read + >30d + not trash; large_attachments: inbox-only + >5MB attachments; duplicates: inbox-only, same lowercased-trimmed subject, groups >2; suggestionIds `news_${hash}`/`large_${hash}`/`dupes_${hash}`), applyCleanup (newsletters → trash; large → folder "archive" + storageTier "cold"; duplicates → archive all but newest by receivedAt; throws "Suggestion not found"), applyAllCleanups, storageForecast (deterministic 1-4MB/day growth; projected 30/60/90 + quotaHitDays + summary), storageDashboard (merges tiers/policy/quota/suggestions/forecast; keys include `suggestionsTotal`, `forecast.summary`, `generatedAt`). Logs to shared `mail_ops_log` (categories storage_policy/tiering/cleanup/cleanup_all).
  - Plumbing: 2 imports + 18 delegations in AdsMarketingModuleService.ts (`mailOps*` + `mailStorage*` — full method list in client.ts after `mailCommandSmartArchive`); 19 routes before `export default router` (`GET|POST /mail/ops/dashboard`, `/mail/ops/incidents`, `/mail/ops/incidents/:incidentId/ack|resolve|escalate`, `/mail/ops/incident-log`, `/mail/ops/retry-queue`, `/mail/ops/housekeeping`, `/mail/ops/checkpoint`, `/mail/ops/threat-rule`, `/mail/ops/log`, `GET /mail/storage/dashboard`, `PUT /mail/storage/policy`, `POST /mail/storage/tiering`, `GET /mail/storage/suggestions`, `POST /mail/storage/suggestions/apply-all`, `POST /mail/storage/suggestions/:suggestionId/apply`); 18 client methods after `mailCommandSmartArchive` (`mailOpsDashboard`, `mailOpsIncidents`, `mailOpsCreateIncident`, `mailOpsAckIncident`, `mailOpsResolveIncident`, `mailOpsEscalateIncident`, `mailOpsIncidentLog`, `mailOpsRetryQueue`, `mailOpsHousekeeping`, `mailOpsCheckpoint(label)`, `mailOpsThreatRule`, `mailOpsLog`, `mailStorageDashboard`, `mailStorageSetPolicy`, `mailStorageRunTiering`, `mailStorageSuggestions`, `mailStorageApplyCleanup(suggestionId)`, `mailStorageApplyAllCleanups`).
  - Frontend: `MailOps.tsx` (/mail/ops — 4 health cards (SMTP/IMAP uptime, queue depth w/ verdict color, error rate), one-click action bar (Retry queue / Run all housekeeping / Take checkpoint / Deploy threat rule), performance/security/business metric grids, 24h alerts w/ severity+status chips, incidents list w/ File modal (P0-P4 severity preset buttons + title/description) + per-incident Escalate/Acknowledge/Resolve, ops activity log, storage tier bar (4 segments w/ counts+bytes) + policy form (auto-tier toggle, cool/cold day inputs, Save policy, Tier now), AI cleanup suggestions w/ Apply + Apply all, forecast summary, 30s auto-refresh toggle + `n0va:refresh-data` listener + skeleton + error card + busy-per-action). Registered in App.tsx (after /mail/command-center) + Sidebar ("Mail Ops", Activity icon, after Mail Command) + CommandPalette.
  - Gates: full backend suite 2278/2278 (97 files); `NovaMailRound7.test.ts` 29/29 stable 4× (6 describes: ops dashboard / incident lifecycle / tiering / cleanup suggestions / forecast & dashboard / one-click ops; tenant `nova-mail7` with fixtures `r7_nl` 40d newsletter, `r7_big` 10MB attachment, `r7_failed` deliveryStatus failed, `r7_frozen` 100d, 3 "Duplicate thread alpha" messages; debug fixes during dev: messagesToday `.messages`→`.totalMessages`, resolveIncident stale-row re-read, newsletter filter `ai?.category || category`, duplicates + large-attachment restricted to inbox folder, housekeeping archive expectation 2→1 after newsletter already trashed); backend tsc — NO new errors (227 pre-existing baseline, verified scoped filter); frontend tsc ZERO errors; `npm run build` GREEN (9.86s). Live smoke (restarted backend via be17.cmd — NOTE: if a stale process holds :4000 the new one dies with EADDRINUSE silently; kill the listening PID first, e.g. `Get-NetTCPConnection -LocalPort 4000 -State Listen` → Stop-Process, then relaunch): ops dashboard (smtp 99.91%/imap 99.93%/queue 0 normal/2 alerts/del 9ms/biz 5 mailboxes 17 msgs 2.83MB), storage dashboard (policy default, tiers hot 14 warm 1, 0 suggestions on seed — expected), incident lifecycle P2 create → escalate (open w/ escalatedTo) → ack (acknowledged) → resolve (resolved + resolvedAt), housekeeping ("1 rule match(es)..."), checkpoint (`cp_...` + label), threat rule (3 rules/97 signatures/13 rescanned), retry ("queue clean"), policy 500 on invalid cool>cold, tiering idempotent. Browse-verified at 375×812: /mail/ops zero overflow zero errors (mobile bottom nav present; overflowEls = 3 benign truncate spans), Run housekeeping click landed server-side ([housekeeping] log entry), File-incident modal via UI created "Browser smoke incident [P2]" (3 incidents total). Browse daemon note: the daemon tab can crash/die (ChildProcess.kill, "Starting server..." spam = client spawned fresh instance = state lost per command) — recover with `Start-Process cmd -ArgumentList '/c', "set BROWSE_SERVER_SCRIPT=dist/server-node.mjs && `"<browse.exe>`" daemon" -WorkingDirectory <workspace root> -WindowStyle Hidden`; state file lives at `<root>\.gstack\terminal-port`; login via the Login page demo form (fill admin@n0va.io/admin123 + Sign In — the demo buttons only prefill); SPA auth reads `localStorage.n0va_token` in App.tsx:167.
- **Round 23 — Search Operators (§3.4.2) + Anti-Abuse Engine (§2.4)** (backend + frontend; `NovaMailRound8.test.ts`, 37 tests green, stable across 3 consecutive runs):
  - `MailSearchOperatorsService.ts` (`mailSearchOp`) — OPERATORS table (15: from, to, subject, has, type, in, label, date, size, is, sentiment, priority, topic, near, related), `parseQuery` (tokenizer `(?:[^\s"]+|"[^"]*")+`, quoted-value stripping `value.replace(/^"|"$/g, "")` — quotes stay in `raw`, unknown-op → invalid + text, `is:` flag whitelist, `size:` via `parseSize` (>=|<=|>|<|= + KB/MB/GB), `date:` via `parseDate` (today/yesterday/last7d/last30d/YYYY/YYYY-MM/YYYY-MM-DD + fallback "recent"), `near:` SLURPS `[m[2], ...tokens.slice(i+1)]` (do NOT include the `near:` token itself), returns `{operators, freeText, filters, invalid, parsed, explanation[], summary}`), `operatorSearch(tenantId, query, opts)` (all 15 filters: from/to/subject `.includes` lowercase, in matches folder OR label, is:important = ai.priority high || importance high, near matches subject+body, related = threadId; free-text scoring subject 10/from 8/labels 4/body 3, sort score desc then receivedAt desc, messages get `score`), logs to `mail_search_log`, `operatorReference`, `searchExamples` (8), `recentQueries` (25-cap slice), `clearHistory` (uses `store.delete` — MemoryStore HAS delete), `operatorStats` (per-op applicable counts + sharePct).
  - `MailAbuseService.ts` (`mailAbuse`) — abuseOverview (5 layers: network/protocol/content/behavioral/quantum with checks/blocked/details; threatLevel low/medium/high via `hashStr(tenantId+"abuse_seed")%60` + spam/bec/honey bumps; counts spamFlagged/becFlagged/honeyHits/sentToday), scanIncoming (deterministic: spamScore = `hash%41` + 12/keyword + 10/2+links + 15/unknown-TLD, cap 100; BEC score `40 + 15*hits`; DLP regexes email/ssn/phone/creditCard/iban with high severity for creditCard/ssn/iban; verdicts: honeyHit→reject, spam≥85 || (BEC≥70 && dlpHigh)→reject, spam≥60→quarantine, `hash%3===1`→greylist, else deliver; returns verdict/threatScore/layers{network,protocol{spf,dkim,dmarc,authScore},content,b,behavioral}/summary), rateLimitCheck (plan limits free100/pro1000/business5000/n0va1o50000 via `tenantPlan` first-mailbox plan; 24h window count + 5-min burst detection `>10 in 5min`, bursts count), becScan (BEC_KEYWORDS + contact-impersonation, confidence `35+14*hits(+20 impersonated)`, flagged ≥55), dlpScan (counts per pattern + severity), impossibleTravel (deterministic lastKnown/attempt/distance/minutes → risk/verdict), sendingPatternAnomaly (sentToday vs `(weekSent-sentToday)/6` avg, anomaly when ratio ≥2.5, mailboxId scope), honeypotStatus (HONEYPOTS = trap@n0va.work/spamtrap@n0va.mail, hits + captured senders), threatResponse (6 actions whitelist: block_sender/quarantine_sender/tighten_greylist/enable_dmarc_enforce/purge_spam/refresh_ip_reputation; throws "Unknown action"), abuseLog (mail_abuse_log sorted desc, 50 cap), abuseDashboard (verdict + layers + counts + honeypots + rateLimits + recentEvents + generatedAt). Collection: `mail_abuse_log`.
  - Plumbing: 2 imports + 19 delegations in AdsMarketingModuleService.ts (after `mailStorageApplyAllCleanups`); 19 routes before `export default router` (`GET /mail/search-operators`, `POST /mail/search-operators/parse`, `GET /mail/search-operators/search?query=&mailboxId=`, `GET /mail/search-operators/examples|recent|stats`, `POST /mail/search-operators/clear`, `GET /mail/abuse/dashboard|overview|rate-limit|impossible-travel|pattern-anomaly|honeypots|log`, `POST /mail/abuse/scan|bec-scan|dlp-scan|threat-response`); 15 client methods after `mailStorageApplyAllCleanups` (`mailSearchOperatorReference`, `mailSearchParse`, `mailSearchOperators`, `mailSearchExamples`, `mailSearchRecentQueries`, `mailSearchClearHistory`, `mailSearchOperatorStats`, `mailAbuseDashboard`, `mailAbuseOverview`, `mailAbuseScan`, `mailAbuseBecScan`, `mailAbuseDlpScan`, `mailAbuseRateLimit`, `mailAbuseImpossibleTravel`, `mailAbusePatternAnomaly`, `mailAbuseHoneypotStatus`, `mailAbuseThreatResponse`, `mailAbuseLog` — 18 total).
  - Frontend: `MailSearch.tsx` got an "Operator search" panel (font-mono input + Run, parse explanation chips + red invalid chips, 15 operator reference chips w/ example tooltips, "Try one" example buttons w/ result counts that run the query, Clear history w/ recent count, operator results list w/ score badges, `n0va:refresh-data` listener); `MailProtection.tsx` new page at /mail/protection (verdict card w/ threat score bar, 5 stat cards (spam blocked/BEC/honeypot hits/daily limit/scanned), 5-layer display w/ per-layer checks bars + blocked counts, one-click defense actions (6 buttons → mailAbuseThreatResponse w/ busy state), honeypot traps + captured senders, recent defense events log, 30s auto-refresh toggle). Registered in App.tsx (after /mail/ops) + Sidebar ("Mail Protection", Shield, after Mail Ops) + CommandPalette.
  - Gates: full backend suite 2315/2315 (98 files); `NovaMailRound8.test.ts` 37/37 stable 3× (7 describes: parser / filtering / reference & history / scanIncoming verdicts / BEC-DLP-travel / rate limits & patterns / honeypots & dashboard; tenant `nova-mail8` with 20+ direct-insert fixtures incl. burst sender (12 msgs in 1 min), sent folders in 2 extra mailboxes for anomaly normal-vs-spike, honeypot-addressed message; debug fixes during dev: fixture helper must spread `...partial` into insert (else `_id` is lost to genId), near slurp semantics, quoted-value stripping, `in:sent` counts ALL mailboxes' sent folders (21 not 10), `size:<1MB` counts 0-byte messages too (38), unread count 15 (incl. 12 burst), free-text "budget" hits 2 (r8_john + r8_sent)); backend tsc — NO new errors (227 pre-existing baseline); frontend tsc ZERO errors; `npm run build` GREEN (1m14s). Live smoke (restarted backend — kill stale :4000 listener first): operators 15, examples 8, search `is:unread has:attachment` → 3 results, abuse dashboard level=high layers=5 honey=0 (seed has no honeypot-addressed mail — expected). Browse-verified at 375×812 (one-shot daemon pattern — see note): /mail/search zero overflow zero errors, mobile nav visible, operator panel fully populated ("15 OPERATORS", 17 recent queries from tests, Try-one examples), Run clicked with `is:unread has:attachment` → "3 operator result" + score badges + summary line; /mail/protection zero overflow zero errors, 10 cards (verdict + 5 stats + layers + actions + honeypots + events). Browse gotchas this round: markers must be case-insensitive — CSS `uppercase` classes make `innerText` markers like "15 OPERATORS" NOT match "15 operators" (`includes` fails); the daemon dies when the launching PowerShell exits even with -WindowStyle Hidden, so run daemon-spawn + full login/nav/eval flow inside ONE bash invocation (state is lost per command otherwise); eval scripts must be plain JS (no TS `as` casts).
- **Round 24 — Mail Webhooks (§4.4) + N0VA1O Mail Agent Framework (§11) + 5 new search operators** (backend + frontend; `NovaMailRound9.test.ts`, 45 tests green, stable across 3 consecutive runs):
  - Extended `MailSearchOperatorsService.ts` (15→20 operators): added `has:voice` (message.flags includes voice_note), `has:poll` (flags poll), `collaborated:with:EMAIL` (mail_comments author match or shared-draft collaborator), `ai:suggested` (ai.summary or ai.priority set), `visual:contains:QUERY` (matches multimodal content-block text) — all in OPERATORS table + filters + explanation; REMOVED the stale `if (op === "has") f.hasAttachment = true` line that suppressed `has:voice`/`has:poll`. `operatorReference().operators` and `operatorStats().stats` now return 20 rows; stats rows include `example` (stats panel/test lookup). NovaMailRound8 assertions updated 15→20.
  - `MailWebhookService.ts` (`mailWebhook` export) — §4.4 webhook registry: WEBHOOK_EVENTS catalog (11 events: message.new, message.sent, campaign.completed, spam.detected, bounce.received, follow_up.due, contact.created, agent.task_created, quarantine.action, schedule.sent, storage.alert), createWebhook (validates event + url `^https?://`, generates `whsec_${random32}` secret, stores in `mail_webhooks`), list/get/update/delete (delete logs removed), pingWebhook (delivery with test payload), fireEvent (finds matching enabled hooks by event, delivers each with deterministic outcome: `hashStr(hook._id|event|joinedEvents) % 7` — 0 = failure (429 timeout), 1 = 500, else success; NEVER random on delivery/gen ids — Round 19 lesson), deliveries (delivery log `mail_webhook_deliveries` w/ status/duration/attempts/responseStatus + body), deliveryStats (successRate, avgDurationMs, deliveries per event, failures), webhookOverview (total hooks, active, deliveries, successRate, events coverage, recentDeliveries, summary), webhookLog. Deliveries payload: `{event, timestamp, data:{...}}` signed via `HMAC_SIGN(timestamp + "." + JSON.stringify(payload))` with `X-N0VA-Signature: sha256=...`.
  - `MailAgentRegistryService.ts` (`mailAgentRegistry` export) — §11 agent framework: AGENT_PERSONAS (10: n0va_executive, n0va_ops, n0va_responder, n0va_campaign, n0va_compliance, n0va_analytics, n0va_calendar, n0va_support, n0va_domain, n0va_discovery — each with name/description/capabilities), AGENT_TOOLS (25: send_email, schedule_send, read_thread, list_inbox, search_mail, draft_reply, summarize, extract_tasks, run_rule, sweep_rules, rescan_spam, run_agent_cycle, archive_old, mark_read, star_message, create_rule, ai_generate_rule, create_campaign, approve_campaign, get_campaign_stats, run_discovery_export, check_domain_health, manage_contacts, run_tiering, run_housekeeping), registerAgent (personaId validation, generates `n0va_ag_${random16}` API key, status active, stores in `mail_agents`), listAgents/getAgent/updateAgent (name/capabilities/personaId)/deactivateAgent, discoverTools (intent keyword matching — "reply"/"respond"→draft_reply+send_email+read_thread; returns `{tools, matched, fallback}`), listSessions/createSession/getSession/endSession (sessions in `mail_agent_sessions` w/ messages + toolCalls), agentAction (tool validation, tools flagged `hitlRequired` (create_campaign, approve_campaign, run_housekeeping, run_tiering, run_discovery_export, create_rule) queue into `mail_agent_hitl` w/ pending status instead of executing; non-HITL tools execute + audit log `mail_agent_audit` w/ result summary + actionsToday increment), hitlQueue/listHitl/resolveHitl (approve → executes tool, status approved, actionsToday increment; reject → rejected; modify → approved w/ custom params), agentDashboard (personas, tools, active agents, sessions, totalActions, hitlPending, audit recent, hitl queue), agentLog. NOTE: agents execute only the deterministic tools (send_email→composeSend, search_mail→searchMessages, run_rule→evaluateAllRules etc.) — no random outcomes.
  - Plumbing: 2 imports + 26 delegations in AdsMarketingModuleService.ts (after `mailAbuseDashboard`); 27 routes before `export default router` (`GET /mail/webhooks`, `POST /mail/webhooks`, `GET /mail/webhooks/overview|log`, `POST /mail/webhooks/ping`, `POST /mail/webhooks/fire`, `GET /mail/webhooks/deliveries|stats`, `/mail/webhooks/:webhookId` GET/PUT/DELETE — static paths BEFORE `:webhookId`; `GET /mail/agents`, `POST /mail/agents`, `/mail/agents/:agentId` GET/PUT/DELETE, `POST /mail/agents/discover`, `POST /mail/agents/sessions`, `/mail/agents/sessions/:sessionId` GET/POST, `POST /mail/agents/:agentId/action`, `GET /mail/agents/hitl|log|dashboard`, `POST /mail/agents/hitl/:hitlId/resolve`); 28 client methods in client.ts (after `mailAbuseLog` — `mailWebhooks`, `mailCreateWebhook`, `mailWebhook`, `mailUpdateWebhook`, `mailDeleteWebhook`, `mailWebhookPing`, `mailWebhookFireEvent`, `mailWebhookDeliveries`, `mailWebhookStats`, `mailWebhookOverview`, `mailWebhookLog`, `mailAgents`, `mailCreateAgent`, `mailAgent`, `mailUpdateAgent`, `mailDeleteAgent`, `mailDiscoverTools`, `mailCreateSession`, `mailSession`, `mailAgentAction`, `mailAgentHitlQueue`, `mailAgentHitlResolve`, `mailAgentDashboard`, `mailAgentLog`).
  - Frontend: `MailWebhooks.tsx` (/mail/webhooks — stat cards, subscription cards w/ event chips + status + secret reveal + ping/delete, create modal w/ event multiselect + URL + description, deliveries list w/ status/response/duration, delivery stats, `n0va:refresh-data` listener); `MailAgents.tsx` (/mail/agents — dashboard cards, persona grid, tool reference, agent cards w/ status + API key copy, register modal w/ persona picker, session panel w/ chat-ish send (agentAction), HITL approval queue w/ Approve/Reject buttons, audit log). Registered in App.tsx (lazy, after /mail/protection) + Sidebar ("Webhooks" Webhook icon, "Mail Agents" Bot icon, after Mail Protection) + CommandPalette (Webhook icon import added there).
  - Gates: full backend suite 2360/2360 (99 files); `NovaMailRound9.test.ts` 45/45 stable 3× (6 describes: operator language 20 / webhook registry CRUD / delivery + HMAC / stats + overview / agent registry + discovery / sessions + HITL + dashboard; tenant `nova-mail9`; 8 initial failures fixed during dev: has:voice filter suppressed by stale `has` handler, `collaborated:with:` parse regex, webhook subscription count reset between tests, HITL daily limit test needs fresh agent, tsc strictness (`as any`/non-null `!`) in test file, webhook secret format assertion); backend tsc — NO new errors (227 pre-existing baseline); frontend tsc ZERO errors; `npm run build` GREEN. Live smoke (restarted backend — kill stale :4000 listener first; token cached at `%TEMP%\opencode\n0va_token.txt`, header `x-tenant-id: tenant_001`): personas=10 tools=25, discover "send reply"→3 tools, webhook create/ping/fire → overview 11 events 100% success, agent register/action/HITL approve/dashboard, operator search `has:attachment is:unread`→3 results. Browse-verified at 375×812 (token-injection flow — see note below): /mail/webhooks zero overflow zero errors (3 benign truncate spans), /mail/agents zero overflow zero errors (overflowEls []), mobile bottom nav visible on both, cards render (webhooks textLen 1267, agents textLen 1763). Browse note: UI login (Sign In) crashes the daemon tab every time — verified working instead via token injection (`storage set n0va_token` while on the localhost:5173 origin, then direct `goto /mail/...`); the login POST endpoint is `/api/v1/auth/login` (NOT `/api/auth/login` — that path 404s) and the SPA unwraps `{data:{token,user}}`.


- **Round 25 - Mail Integrations hub (§9)** (backend + frontend; NovaMailRound10.test.ts, 37 tests green, stable across 3 consecutive runs):
  - MailIntegrationService.ts (mailIntegration) - CONNECTORS (10: gmail, outlook, slack, teams, crm, drive, dropbox, zoom, calendar, notion - each with id/name/description/category/scopes/actions/authType), INTEGRATION_CATEGORIES (6: email/chat/crm/storage/meetings/docs), BRIDGE_EVENTS (7: mail.received, mail.sent, mail.spam_detected, mail.delivery_failed, mail.rule_triggered, mail.attachment_scanned, mail.ai_suggestion). Methods: connectorCatalog, listConnections, getConnectionPublic, connectConnector (throws "connector not found"/"mailboxId is required to connect"/"Mailbox not found"; **reconnect semantics: existing connected = "Already connected", existing non-connected record = reconnect (status to connected, reset lastSync/error, new 45-day token, logs connection_reconnected)**), authorizeConnector (no-op "already authorized" when connected), refreshConnector (throws when disconnected), updateConnection, disconnectConnector (status disconnected + error null, logs), syncNow (deterministic outcome `hashStr(\`${connectionId}|sync\`) % 11 === 0` to failed - 0 = failure, else success; itemsSynced `3 + hashStr % 12`; durationMs `120 + hashStr % 3800`; `sync_mail` additionally snapshots mailbox messages count + `contacts_pushed` on sync_contacts), syncHistory (mail_integration_jobs sorted desc, 25 cap), syncStatus (`{connectors: [...], overdue: [...], summary}` - per-connection lastSyncAt/lastSyncStatus/overdue(>24h)/status), runAction (validates connector supports action - throws "does not support action"; 9 actions: sync_mail, create_event, post_to_chat, forward_to_channel, push_to_crm (lead score 20-89 via hash), sync_contacts (counts tenant mail_contacts rows), upload_file (needs message w/ attachment - else "No attachment found"), schedule_meeting (https://.n0va.link/meet), create_task), integrationLog (mail_integration_log sorted desc, 50 cap), integrationAlerts (high = log error, medium = needs_auth, low = stale >24h), integrationOverview (10 connectors w/ status/category/actions, connected, itemsSynced, actionsRun, bridges, alerts, summary), integrationDashboard (merges overview + recent jobs + recent log + generatedAt), listBridges/createBridge (name + event + connectorId + action + target required; connector must exist - else "Connector not found")/deleteBridge/triggerBridge (requires active connected connector of that type - else "No connected {name} connection found"; always fires + logs ridge_fired; returns matchedConnectors + fired + summary). Collections: mail_connections, mail_integration_jobs, mail_integration_log, mail_integration_bridges. NOTE: connection rows include connectionId: c._id via toPublic (no duplicates - watch TS2783 if spreading extra connectionId key).
  - Plumbing: 1 import + 19 delegations in AdsMarketingModuleService.ts (after mailAgentDashboard); 19 routes before export default router (static paths BEFORE :connectionId: GET /mail/integrations/catalog|status|syncs|log|alerts|overview|dashboard|bridges, POST /mail/integrations/connect|bridges, DELETE /mail/integrations/bridges/:bridgeId, POST /mail/integrations/bridges/:bridgeId/trigger, GET /mail/integrations, POST /mail/integrations/:connectionId/disconnect|authorize|refresh|sync|actions, PUT /mail/integrations/:connectionId); 18 client methods in client.ts (after mailAgentResolveHitl - mailIntegrationCatalog, mailIntegrationConnections, mailIntegrationConnect, mailIntegrationDisconnect, mailIntegrationAuthorize, mailIntegrationRefresh, mailIntegrationUpdate, mailIntegrationSyncNow, mailIntegrationSyncHistory, mailIntegrationSyncStatus, mailIntegrationAction, mailIntegrationLog, mailIntegrationAlerts, mailIntegrationOverview, mailIntegrationBridges, mailIntegrationCreateBridge, mailIntegrationDeleteBridge, mailIntegrationTriggerBridge).
  - Frontend: MailIntegrations.tsx (/mail/integrations - 5 stat cards (Connected/Need auth/Errors/Syncs/Items synced), alerts banner, 10-connector grid w/ status chip + per-connection sub-cards (account email, sync/refresh/2 quick actions/authorize/disconnect, last sync, error text), bridges automation list w/ event chip -> connector action + fire/delete, connect modal (mailbox select from mailMailboxes()), bridge modal (event/connector/action/target w/ action options filtered by connector), recent syncs + integration log columns, 30s auto-refresh toggle, 
0va:refresh-data listener, skeleton + error card). Registered in App.tsx (lazy, after /mail/agents) + Sidebar ("Mail Integrations", Plug icon - was NOT imported before, after Mail Agents) + CommandPalette (Plug added to its lucide import).
  - Gates: full backend suite 2397/2397 (100 files); NovaMailRound10.test.ts 37/37 stable 3x (6 describes: connector catalog / connect-disconnect lifecycle / sync jobs / integration actions / alerts & overview / integration bridges; tenant 
ova-mail10); backend tsc - NO new errors (0 in Round 25 files after fixes; 227 pre-existing baseline; **fixes during dev: 4x TS2783 'connectionId specified more than once' from spreading toPublic() with a leading connectionId: key - removed the duplicate keys; 8x TS18048 'g is possibly undefined' in test from .find() - added s any; a PowerShell Set-Content round-trip without -Encoding UTF8 corrupted 20 em dashes to â€" mojibake - repaired via char-code Replace, verify with ([regex]::Matches($c,'â€')).Count = 0); frontend tsc ZERO errors (lucide-react has NO `Sync` export - use `RotateCw`; dynamic `api.adsMarketingModule[...]` indexing fails tsc - use explicit if/else branches); `npm run build` GREEN (~1m4s, chunk-size warning only). Live smoke (restarted backend - kill stale :4000 listener first): catalog 10 connectors/6 categories, connect slack (2 scopes authorized), sync (8 items), post_to_chat (Posted "hello" to #smoke), bridge create + fire, overview 1 connected/8 items/1 action/1 bridge/0 alerts - all OK. Browse-verified at 375x812 (one-shot token-injection flow): /mail/integrations zero overflow zero errors (overflowEls [], err null, hasCards true, textLen 1783), - **Round 26 - Mail Billing & Quota Management (§3.3) + Mail Notifications Center** (backend + frontend; NovaMailRound11.test.ts, 33 tests green, stable across 3+ runs):
  - `MailBillingService.ts` (`mailBilling`) - PLAN_ORDER free/pro/business/n0va1o (0/12/49/199), 9 limit dimensions (mailboxes/storageBytes/contacts/messagesPerDay/domains/webhooks/agents/campaigns/integrations; n0va1o storage 5TB - was 5GB bug, edited), tenantState (30-day cycle), computeUsage/usageRows, billingSummary (status critical when any dimension over limit, recommendedPlan via PLAN_ORDER scan), upgradePlan (prorated charge priceDiff*(daysLeft/30), open invoice INV-{1000+count+1}), downgrade blocked by usageFits, invoices/getInvoice/payInvoice, paymentMethods CRUD (default flag), usageForecast (~4MB/day growth, quotaHitDays), log/billingLog (mail_billing_log), billingDashboard (summary+usage+invoices+cards+forecast+log+generatedAt). Collections: `mail_billing`, `mail_invoices`, `mail_payment_methods`, `mail_billing_log`. Deterministic via hashStr(tenantId+suffix).
  - `MailNotificationService.ts` (`mailNotifications`) - NOTIFICATION_TYPES 12 (rule_triggered/follow_up_due/snooze_expired/campaign_pending/spam_detected/storage_warning/ops_alert/webhook_failed/domain_flag/agent_hitl/integration_error/invoice_due) + TYPE_LABELS/TYPE_SEVERITY/TYPE_LINKS, createNotification w/ dedupe (tenantId+type+key+!read -> {created:false, reason:'duplicate'}), collectAlerts scans 11 sources (mail_follow_ups due, messages snoozedUntil, mail_campaigns pending_approval, mail_spam_log today, mailboxes percentUsed>=75, mail_ops_incidents open, mail_webhook_deliveries failed, mail_domains action_required w/ pending=6-verifiedCount, mail_agent_hitl pending_review, mail_connections error|needs_auth, mail_invoices open due), settings per type (disabled/skipped counts), markRead/markAllRead/delete/clear, settings get/update, notificationCenter (unread+total+settings), notifications list w/ unreadOnly/type/limit. Collections: `mail_notifications`, `mail_notification_settings`. `added` entries carry {type,key,title,message}.
  - Plumbing: 2 imports + 24 delegations in AdsMarketingModuleService.ts (after mailIntegration block); 24 routes before `export default router` (`/mail/billing/plans|summary|dashboard|forecast|upgrade`, `/mail/billing/invoices` GET/POST + `/:invoiceId` + `/:invoiceId/pay`, `/mail/billing/payment-methods` GET/POST + `/:methodId` DELETE, `/mail/billing/log`; `/mail/notifications` GET/POST + `/collect` + `/read-all` + `/clear` + `/settings` GET/PUT + `/center` + `/:notificationId/read` + `/:notificationId` DELETE); ~30 client methods after `mailIntegrationTriggerBridge` (`mailNotifications` builds URLSearchParams).
  - Frontend: `MailBilling.tsx` (/mail/billing - plan cards, usage-vs-limits bars, storage forecast, payment methods, invoices w/ Pay, billing activity log, Add card modal, 30s auto-refresh toggle, `n0va:refresh-data` listener), `MailNotifications.tsx` (/mail/notifications - stat cards, type filter chips, notification list w/ severity dots + links + mark-read/delete, per-type settings toggles, Scan for alerts / Mark all read / Clear all, 30s auto-refresh). Registered in App.tsx (lazy, after /mail/integrations) + Sidebar (Mail Billing CreditCard, Mail Alerts Bell) + CommandPalette.
  - Gates: full backend suite 2430/2430 (101 files); NovaMailRound11.test.ts 33/33 stable 3x (unique tenants `nova-mail11_{n}` - MemoryStore has NO clear(); invoice description in truncate line drove grid min-content to 470px -> fixed with `min-w-0` on the two grid cards, mainSw now 375==375); backend tsc - NO new errors (261 pre-existing baseline lines, none in new files); frontend tsc ZERO errors; `npm run build` GREEN (~14s). Live smoke (backend restarted via be26.cmd running `node.exe ..\..\node_modules\tsx\dist\cli.mjs src/index.ts` from n0va\packages\backend with hermes node; kill stale :4000 listener first): billing summary free/critical/recommended pro, upgrade -> "Plan changed to Pro - prorated charge $12.00", card visa 4242 default, forecast 25600 days, collect/center clean on seed tenant ("0 alert(s) created"), settings 12 types true. Browse-verified at 375x812 (daemon died mid-session -> relaunched via `cmd /c "set BROWSE_SERVER_SCRIPT=dist/server-node.mjs && browse.exe daemon"` hidden; token re-injected via `browse storage set n0va_token <token>` after goto landed on /login): /mail/billing fixed (mainSw 375==375, cards 166/343, err null), /mail/notifications zero overflow zero errors (overflowEls [], textLen 627, mobile nav 375x60 at bottom 812 visible).
- **Round 27 - Mail Dispatch page** (frontend only; no backend changes): new `MailDispatch.tsx` at `/mail/dispatch` — a one-screen execution view combining queued sends, follow-up reminders and campaign send windows: 4 stat cards (scheduled sends + due-today, follow-ups due + overdue, awaiting response + snoozed, campaign approvals + active), "Schedule a send" inline form (mailbox select + to + subject + datetime-local → `mailScheduleSend`), scheduled queue w/ Send-now (`mailSendScheduleNow`) + Cancel (`mailCancelSchedule`), open follow-ups w/ Done (`mailCompleteFollowUp`) + Delete, campaign rows w/ status-appropriate actions (Launch `mailLaunchCampaign`, Approve/Reject `mailApproveCampaign`/`mailRejectCampaign`, Pause/Resume `mailPauseCampaign`/`mailResumeCampaign`), totals chips, 30s auto-refresh toggle + `n0va:refresh-data` listener + skeleton + error/empty states + busy-per-action. Registered in App.tsx (lazy route after /mail/command-center) + Sidebar ("Mail Dispatch", SendHorizonal icon — new import, after Mail Command) + CommandPalette.
  - Debug fixes during dev: (a) `listMailboxes` rows expose `mailboxId` NOT `_id` — option value fell back to the name text → backend "Mailbox ... not found"; fixed with `value={m.mailboxId || m._id}`; (b) create form used the generic `act()` helper which closed/reset the form even on failure — replaced with inline try/catch that only resets on success.
  - Gates: frontend tsc ZERO errors; `npm run build` GREEN (~13.6s); no backend changes so no new tests (suite unchanged 2430/2430). Live smoke (existing :4000 backend): schedule create via UI → row appears, Send-now → status [sent] on backend, empty state → populated. Browse-verified at 375x812: /mail/dispatch zero doc overflow (scrollWidth 375 == clientWidth 375, horizontalOverflow false), overflowEls = 4 benign truncate spans (same pattern as /mail/ai), err null, mobile bottom nav visible (375x60 at y752), full create→queue→send-now flow driven through the UI (select/fill/click).
- **Round 28 - Mail Calendar page** (frontend only; no backend changes): new `MailCalendar.tsx` at `/mail/calendar` — a monthly execution timeline combining 4 item types (scheduled sends from `mailListScheduled`, open follow-ups from `mailFollowUps({status:"open"})`, campaign send windows from `mailCampaigns` [only rows with `scheduleAt`], snoozed messages from `mailListSnoozed`): month navigation (prev/next/Today), 42-cell 7-column grid with today highlight + selected-day ring, color-coded legend (send n0va / follow-up amber / campaign violet / snoozed gray), desktop shows up to 3 title chips per cell (+N more), mobile shows up to 5 dots (+N count), click a day → detail panel with per-item quick actions (Send now `mailSendScheduleNow`, Cancel, Done/Delete `mailCompleteFollowUp`/`mailDeleteFollowUp`, Approve/Reject `mailApproveCampaign`/`mailRejectCampaign`, Pause/Resume, Unsnooze `mailUnsnooze`), month item-count badge, empty state, 30s auto-refresh toggle + `n0va:refresh-data` listener + skeleton + busy-per-action. Registered in App.tsx (lazy route after /mail/dispatch) + Sidebar ("Mail Calendar", CalendarClock icon — new import, after Mail Dispatch) + CommandPalette.
  - Gates: frontend tsc ZERO errors; `npm run build` GREEN (~10.5s); no backend changes so no new tests (suite unchanged 2430/2430). Live smoke (existing :4000 backend): 42 cells render, month label + legend + today ring correct, Aug 5 cell carries the n0va dot for the scheduled send, day-detail panel shows "Direct POST test 14:30 · to direct-test@partner.com" with chips, Send-now from the calendar flipped status → [sent] on backend. Browse-verified at 375x812: zero doc overflow (scrollWidth 375 == clientWidth 375), overflowEls = 1 benign truncate span (detail-row meta), err null, mobile nav visible (375x60). Browse note: daemon died mid-session again — relaunch via `Start-Process cmd /c "set BROWSE_SERVER_SCRIPT=dist/server-node.mjs && browse.exe daemon"` hidden + token re-inject.
, Connect modal opens w/ mailbox dropdown populated (Jane Personal/Work/Team Inbox...).

- **Round 29 - Mail Reader page** (frontend only; no backend changes): new `MailReader.tsx` at `/mail/reader` — a split-view threaded reading experience: two-pane layout on lg (`lg:grid-cols-[340px_minmax(0,1fr)]`, list + reader side-by-side) with full pane swap on mobile (reader replaces list, back chevron `lg:hidden`); list pane has folder chips w/ unread counts + client-side search (subject/sender/body) + per-row avatar initials (deterministic color), unread dot, star (hover on lg), snippet, attachment icon, paperclip; reader pane shows the full thread (all messages w/ avatars + Ani AI summary + attachments) with toolbar (Star/Archive/Trash), inline Reply (prefilled `Re:` + quoted body with `>` prefix, mailbox select, Ctrl+Enter hint) and Forward (`Fwd:` + forwarded-message block) forms — no modal, AI smart reply chips (`mailSmartReply` tones concise/friendly/professional, tap-to-fill), snooze presets (Later today/Tomorrow/Next week), J/K keyboard navigation (guarded against inputs) + Esc closes; swipe gestures on list rows (touchstart/touchmove/touchend, translateX visual w/ Archive/Trash bg layers, `<-60px` = archive, `>60px` = trash); 30s auto-refresh toggle + `n0va:refresh-data` listener + skeleton + empty state + busy-per-action. Registered in App.tsx (lazy route after /mail/calendar) + Sidebar ("Mail Reader", MailOpen icon — new import, after Mail Calendar) + CommandPalette.
  - Gates: frontend tsc ZERO errors; `npm run build` GREEN (~13.5s); no backend changes so no new tests (suite unchanged 2430/2430). Live smoke (existing :4000 backend): mobile list renders 13 rows w/ swipe layers, row click → pane swap + thread ("1 message(s) · John Smith"), inline reply sent ("Re: Q3 invoice attached" → john.smith@partner.com confirmed in sent folder), AI chips render + tap fills the reply form, swipe-left via synthetic TouchEvents archived the message (rows 13→12, backend archive contains "Q3 invoice attached"), J key jumped to next thread (Sarah Chen), desktop 1366×768 split view (list 340px @ x:288 + reader 690px @ x:644) with zero doc overflow. Browse-verified at 375x812: zero doc overflow (scrollWidth 375 == clientWidth 375), overflowEls all benign (truncate snippets + scrollable folder-chip row), err null, mobile bottom nav visible (375x60 at y752). Browse notes: swipe must be tested in SEPARATE evals (touchstart/touchmove then touchend — React batches state across a single eval so touchend sees stale swipe state); `document.querySelector("aside")` matches the app sidebar first — target the pane via the folder-chip row's ancestors.
- **Round 30 - remaining §3.3 billing surface** (backend + frontend; `NovaMailRound12.test.ts`, 26 tests green, stable across 3 runs):
  - Extended `MailBillingService.ts` (`mailBilling`) — ADDONS catalog (storage_100gb $5, storage_500gb $20, domain_5 $6, domain_20 $20, mailbox_10 $10, contact_1000 $4) w/ `effectiveLimits` (storage addons × GB) applied to usage rows + forecast; addAddon (prorated invoice kind "addon") / removeAddon (prorated refund credit kind "refund") / addonCatalog / listAddons; OVERAGE_RATES (9 dimensions: storageBytes 2, mailboxes 3, contacts 0.01, messagesPerDay 0.001, domains 5, webhooks 0.5, agents 2, campaigns 1, integrations 1) w/ overagePolicy (warn/block/bill, PUT) + overageStatus (projected cost = overUnits × rate × daysLeft/30) + overageInvoice (bill mode only); contracts (12/24/36 mo, annualPrice, discountPct, seats, paymentSchedule) w/ createContract / contractStatus (effectiveMonthly = annual/12, daysToRenewal) / cancelContract (also nulls billing.contractId — re-checked via `status !== "active"`); usageAlerts (critical over-limit / warning ≥ threshold, per-dimension thresholds, DEFAULT_ALERT_THRESHOLD 85) + setAlertThresholds; downgradePlan (blocks when effective limits violated — **display now shows TARGET plan limit, e.g. "(5 / 1)" not "(5 / 5)"; same fix in upgradePlan's downgrade branch — both use `effectiveLimits({...state, plan: target})` incl. addons**, prorated refund credit) + upgradePlan downgrade-branch fix; setAutoRenew / cancelSubscription (status "canceling"); creditBalance (negative invoices). `billingDashboard` now exposes `effectiveMonthly`, `addonMonthlyTotal`, addons, addonCatalog, overage, contract, alerts, credits, autoRenew; `billingSummary.nextInvoice.amount` = effectivePrice + addonTotal. **Bug fixed during live smoke: dashboard omitted `effectiveMonthly`/`addonMonthlyTotal` (present in summary but not spread) — added + regression test.**
  - Plumbing: 18 delegations in AdsMarketingModuleService.ts (after `mailBillingLog` — `mailBillingOveragePolicy` passes null through); 19 routes in ads-marketing-module.ts (POST downgrade/auto-renew/cancel, GET addons + GET addons/active, POST/DELETE addons/:addonId, GET/PUT overage, POST overage/invoice, GET/POST contracts, GET contracts/status, POST contracts/cancel, GET/PUT alerts, GET credits); 17 client methods in client.ts (after `mailBillingLog:`).
  - Frontend: `MailBilling.tsx` extended — plan-card row now shows `effectiveMonthly` (contract-aware) + addon total + auto-renew off warning; 4 new cards (Add-ons storefront w/ Active chips + remove, Overage policy w/ mode select + projected cost rows + Invoice overage, Enterprise contract w/ create form + active summary + cancel, Usage alerts w/ toggle + severity list); plan row action bar (Downgrade select + auto-renew toggle + Cancel subscription); invoice rows show kind chips (addon/overage/refund) + credit amounts. 2×2 sections grid kept `min-w-0` (Round 26 overflow fix preserved). Route/nav unchanged (/mail/billing already registered).
  - Gates: full backend suite 2456/2456 (102 files); `NovaMailRound12.test.ts` 26/26 (7 describes incl. new dashboard effectiveMonthly regression); backend tsc — NO new errors; frontend tsc ZERO errors; `npm run build` GREEN (~7.7s). Live smoke (restarted backend): addon add "prorated $5.00" → dashboard addons 1 (+5/mo), overage mode bill, contract "Smoke Corp $300/yr → $25/mo effective" + nextInvoice $30, downgrade blocked "Mailboxes usage (5 / 1) exceeds the plan limit" (after fix), auto-renew off, overage invoice INV-001002 $12.00. Browse-verified at 375×812: zero overflow (1 benign truncate), err null, mobile bottom nav visible (375x60 @ y752), all 6 new sections render, Add addon via UI click → confirm auto-accepted → POST 200 → page auto-refresh → "Active: 1" chip + 5 Add buttons (dashboard payload grew 5446→6220); desktop 1366×768 zero overflow overflowEls [].
- **Round 31 - eDiscovery & Compliance deep-dive** (backend + frontend; `NovaMailRound13.test.ts`, 19 tests green, stable across 3+ runs; full suite 2475/2475, 103 files):
  - Extended `MailDiscoveryService.ts` (`mailDiscovery`) — CONCEPT_TOPICS (7: contract/legal, invoice/finance, meeting, hr, finance/budget, marketing, personal — each w/ label + keywords) + conceptSearch (scored clustering: subject keyword +2 / body +1, best ≥2 wins else general, clusters `{topic,label,score,count,messages(first 20, w/ messageId/from/date/folder/score)}`, summary; empty query = cluster everything); PRIVILEGE_TYPES (4: attorney_client, work_product, settlement, confidential) + markMessagePrivileged (throws on unknown message/type, no reason, or already-privileged; inserts into `mail_privileges` w/ assertedBy) / listPrivileges (`{privileges, count, summary}`, rows have privilegeId/messageId/type/typeLabel/subject/from/reason/assertedBy/createdAt) / removePrivilege (soft-removes `removed: true`) / privilegeSummary (`{total, byType: [{type,label,count}]}`); exportAuditChain (re-hashes every export's content: expected = hashStr(prevHash + contentHash), `verified` per entry, `chainIntact` + `brokenAt` on first mismatch, entries reversed chronologically — 0 exports = intact "0 export(s) hash-verified"); createExport now chain-stamps `previousHash`/`chainHash` (GENESIS for first) + `kind: "ediscovery"`; discoverySummary extended (privileged count, chainIntact, summary).
  - Extended `MailComplianceService.ts` (`mailCompliance`) — placeHold extended w/ startDate/endDate (optional, validated); legalHoldCalendar(tenantId, month?) (42 cells `{date, day, events: [{holdId, subject, reason, released, type}]}`, type = placed/expiring (endDate === date)/active (within range)/released; top-level `{month, monthLabel, daysInMonth, startOffset, cells, activeHolds, expiringSoon, placedThisMonth, summary}` — NO per-cell hasEvents/isToday keys; throws on malformed month); REPORTS map (GDPR/CCPA/HIPAA w/ label + description) + frameworkChecks (per-framework 5 checks computed from PII scan/retention policies/holds/audit log + deterministic CCPA opt-out/delete counts + HIPAA PHI/encryption rolls) + complianceReport (score = pass 1 / warn 0.5 weighted, status pass ≥80 / warn ≥50 / fail; `{framework, label, description, score, status, checks: [{key,name,status,detail}], passing, failing, summary, generatedAt}`) / complianceReports (`{reports: [{framework,label,score,status}], average, summary}`) / exportComplianceReport (writes text report, **also inserts `mail_exports` record kind `compliance_report` + `format: "report"` that JOINS the export audit chain**, audits `compliance_report_export`).
  - Plumbing: 10 delegations in AdsMarketingModuleService.ts (4 compliance after `mailComplianceSummary`: holdCalendar/reports/report/exportReport; 6 discovery after `mailDiscoverySummary`: conceptSearch/markPrivileged/privileges/removePrivilege/privilegeSummary/exportChain); 13 routes before `export default router` (GET /mail/compliance/hold-calendar?month=, GET /mail/compliance/reports, GET + POST /mail/compliance/report/:framework, POST /mail/compliance/report/:framework/export, GET /mail/discovery/concept-search?query=&limit=, POST /mail/discovery/privileges, GET /mail/discovery/privileges, DELETE /mail/discovery/privileges/:messageId, GET /mail/discovery/privilege-summary, GET /mail/discovery/export-chain); 10 client methods in client.ts.
  - Frontend: `MailDiscovery.tsx` extended — Concept search card (input + Cluster → 3-col cluster grid w/ count badges + message subjects), Privilege log card (Assert modal: messageId/type select w/ 4 PRIV_TYPES labels/reason; rows w/ typeLabel + reason + remove), Export audit chain card (chainIntact banner green/red + per-export VERIFIED/TAMPERED chips + chain hash prefix), loadAll now fetches privileges + chain too; `MailCompliance.tsx` extended — Legal hold calendar card (42-cell 7-col grid, prev/Today/next nav via shiftMonth helper, today ring `c.date === todayStr`, ≤3 event dots/type via CAL_EVENT_STYLE + legend, header shows `cal.activeHolds · expiringSoon.length ≤7d · placedThisMonth` — backend has NO top-level events object), Compliance report packs card (average readiness line + per-framework rows w/ score + status chip + Open), report detail modal (label/description/status banner pass|warn|fail, checks w/ colored dots + `c.name` + detail, Export button → mailComplianceExportReport), hold modal extended w/ optional start/end date inputs. **Debug fixes during browse: privilege list needs `mailDiscoveryPrivileges()` (NOT privilegeSummary which returns byType counts only); calendar cells have no hasEvents/isToday — compute from `(c.events||[]).length` + date compare; report rows use `label` (not name); checks use `name`; status values pass/warn/fail (not compliant/partial).**
  - Gates: full backend suite 2475/2475 (103 files, 3 consecutive stable runs after one transient first-run 2474/2475); `NovaMailRound13.test.ts` 19/19 (5 describes: concept search / privilege lifecycle / export audit chain / legal hold calendar / compliance reports; tenants `nova-mail13_{N}` w/ direct-insert fixtures + explicit `_id`s; test bugs fixed during dev: released-hold calendar test used RELATIVE dates (2026-06 was expired at runtime) — pinned to today-based dates; 4 TS possibly-undefined casts); backend tsc — NO new errors; frontend tsc ZERO errors; `npm run build` GREEN (~8.6s). Live smoke (restarted backend — stale :4000 had 404s on new routes): hold w/ startDate+endDate → calendar "1 active · 1 placed this month" (current month) + "1 active" in Nov-2026 month view; privilege assert on "Q3 invoice attached" (attorney_client) → list/remove; export "Smoke package" 17 msgs PII-redacted → chain 1 entry intact; GDPR report export → chain 2 entries `compliance_report,ediscovery` intact; GDPR score moved 60→70 after hold placed (hold check warn→pass). Browse-verified at 375×812: /mail/discovery (scrollWidth 375 == clientWidth, 2 benign truncate spans, err null, cluster run via UI → "Finance & budget | 1 | Budget approval needed", privilege row renders "Q3 invoice ...", chain card shows hash-verified) + /mail/compliance (zero overflow, 12 benign truncate audit spans, calendar 49 divs (7 headers + 42 cells) + today ring + "2026-08 · 1 Active · 0 Expiring ≤7d · 1 Placed", report packs avg 86.67% GDPR WARN 70 / CCPA PASS 90 / HIPAA PASS 100, GDPR modal open/close via UI shows 3/5 checks passing w/ details); desktop 1366×768 both pages zero overflow overflowEls [].
- **Round 32 - Quantum security + Collaboration v2** (backend + frontend; `NovaMailRound14.test.ts`, 31 tests green, stable across 3 runs; full suite 2506/2506, 104 files):
  - `MailQuantumService.ts` (`mailQuantum`) — PQC_ALGORITHMS export (6: kyber_1024/dilithium_5/falcon_512/sphincs_256f NIST-standardized + frodokem_976/mceliece_8192128 finalist, each w/ family/type/securityBits/keyBytes/note); QKD_CHANNELS (5); algorithmCatalog; createKeyPair (default kyber_1024/email_encryption, fingerprint `fp_${hashStr(tenant|algo|purpose|label).toString(36).toUpperCase()}`, label default `${purpose}@${algo.id}`)/listKeys/getKey/rotateKey (retires old w/ previousFingerprint + inserts fresh w/ rotatedFrom + logs key_rotation)/revokeKey (logs key_revoked); createQkdChannel (deterministic distanceKm `50 + hash%750`, errorRatePct `(hash%30)/10 + 0.5`, keyRateKbps `100 + hash%900`, securityLevel "quantum-safe")/qkdChannels (totalKbps)/simulateQkdExchange (seed = tenant|channelId|distanceKm; sentPhotons `10000 + hash%900000`, detected = sent × (1−QBER), sifted = detected × (0.5 + hash%40/100), distilled = sifted × 0.8, eavesdropCheck clean unless `hash%7 === 0`; updates channel lastExchangeAt + logs qkd_exchange)/qkdExchanges (25 cap, totalDistilled); encryptMessage (plaintext required; deterministic seed tenant|algo|plaintext; cipherId `cph_${hash.toString(36)}${random6()}`, capsule `kc_`, bound to active key w/ matching algorithm, ciphertextBytes `max(len*2+96, 256)`, overheadPct 100 — returns keyId too)/decryptMessage (finds by cipherId; throws "not found"); issueCertificate (commonName + positive validityDays required — `validityDays !== undefined ? value : "365"` NOT `|| "365"` (0 swallowed); issuer "N0VA-QCA", serial/fp via hashStr(tenant|cn|algo), chainDepth 0)/listCertificates/getCertificate/revokeCertificate (logs cert_revoked)/renewCertificate (logs cert_renewed); certificateChain (root "N0VA Quantum Root CA" depth 2 + intermediate "N0VA-QCA Intermediate" depth 1 + leaves; verified = all leaves active); quantumOverview (4 readiness layers keys/qkd/certs/encrypted-mail, needed = ceil(messages/4|8|6|2), readiness avg, level at_risk <40 / transitioning <75 / hardened ≥75, recommendations built from empty layers); quantumDashboard (spread overview + keys + channels + certificates + recentLog + generatedAt); quantumLog (`mail_quantum_log`). Collections: `mail_quantum_keys`, `mail_qkd_channels`, `mail_qkd_exchanges`, `mail_quantum_messages`, `mail_pqc_certs`.
  - `MailCollabV2Service.ts` (`mailCollab2`) — approvals: createApproval (subject required, approvers non-empty, requiredCount between 1 and approvers.length, approver emails lowercased, rows status pending)/approveApproval (non-approver throws; already-final throws; auto-approved when approvedCount ≥ requiredCount w/ approvedAt)/rejectApproval (locks to rejected)/withdrawApproval (pending only)/listApprovals(status?)/approvalDashboard (pending/approved/rejected/approvalRate/avgTurnaroundHours `hash%48`/overdue); delegation: delegateInbox (mailboxId + existing mailbox + granteeEmail + permission read|respond|admin required; duplicate only when existing active OR accepted)/acceptDelegation (revoked throws; idempotent when accepted)/revokeDelegation/listDelegations (mailboxId/grantee filters)/delegationSummary (byPermission/byMailbox); team roles: TEAM_ROLES 4 (owner/admin/editor/viewer w/ permission lists) + assignRole (member required, role whitelist; existing member → updated: true)/listRoles/removeRole/roleMatrix/teamDashboard (members, byRole counts, coveragePct `min(100, members/5*100)`); mentions: mentionDetect (regex `@([A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,})|@([A-Za-z0-9_\-\.]{2,24})`, name match via exact OR startsWith — "Sarah" resolves "Sarah Chen")/createMention (contextType whitelist comment|draft|approval|shared_inbox + contextId required; **skips unresolved mentions even when they carry an email**; throws "No mentionable recipients found" when 0 created)/listMentions(unreadOnly/contextType/limit)/markMentionRead/mentionsSummary (unread + byContext); collab2Dashboard (merges all + attentionScore = pending approvals + pending delegations + unread mentions). Collections: `mail_approvals`, `mail_delegations`, `mail_team_roles`, `mail_mentions`.
  - Plumbing: 2 imports + 42 delegations in AdsMarketingModuleService.ts (mailQuantum* then mailCollab2* after mailNotificationCenter); 41 routes before `export default router` (`/mail/quantum/algorithms|overview|dashboard|log|chain|keys[+ :keyId + /rotate|revoke]|qkd-channels[+ :channelId/simulate]|qkd-exchanges?channelId=|encrypt|messages/:messageId/decrypt|certificates[+ :certId + /revoke|renew]`; `/mail/collab2/dashboard|approvals[+ /dashboard + :approvalId/approve|reject|withdraw]|delegations[+ :delegationId/accept|revoke]|delegation-summary|roles[+ /matrix + DELETE :roleId]|team-dashboard|mentions/detect|mentions[+ :mentionId/read]|mentions-summary`); 34 client methods in client.ts (after `mailNotificationCenter:` — `mailQuantum*` × 15 + `mailCollab2*` × 19). Debug fixes during tests: `toPublic` needed previousFingerprint/retiredAt/rotatedFrom; encryptMessage response needed keyId; `|| "365"` swallowed validityDays 0; delegation duplicate check needed accepted status too; mention name match needs startsWith; createMention must skip unresolved targets.
  - Frontend: `MailQuantum.tsx` new page at /mail/quantum (readiness % + level badge, 4 stat cards, readiness layer bars w/ pct + active/needed, recommendations list, 6-algorithm catalog w/ type chips, key vault w/ Create modal (algorithm/purpose/label) + Rotate/Revoke, QKD channels w/ Create modal + Run key exchange + recent exchanges (distilled B + QBER + eavesdrop dot), PQC certs w/ Issue modal (CN/algorithm/validityDays) + Renew/Revoke, cert chain tree w/ verified banner, recent quantum events log, `n0va:refresh-data` listener + skeleton); `MailCollaboration.tsx` extended — 4 collab2 stat cards (approvals pending/delegations/team members/unread mentions), Approval workflows card (new-modal subject/approvers/requiredCount/reason, per-row Approve/Reject/Withdraw, rate chips), Inbox delegation card (delegate modal w/ mailbox select + grantee + permission read|respond|admin, Accept/Revoke), Team roles card (assign modal member/role, remove, permission matrix chips), Mentions card (mention input + list w/ unread ring + Mark read). Registered in App.tsx (lazy, after /mail/notifications) + Sidebar ("Mail Quantum", Lock icon — new import, after Mail Alerts) + CommandPalette (Lock added to lucide import).
  - Gates: full backend suite 2506/2506 (104 files); `NovaMailRound14.test.ts` 31/31 stable 3× (5 describes: PQC key management / QKD channels / encryption + certificates / approval workflows / delegation-roles-mentions; tenants `nova-mail14_{N}`; hashStr replicated in test to pin deterministic values — distanceKm/QBER/keyRateKbps/QKD exchange pipeline/avgTurnaroundHours/coveragePct 40); backend tsc — NO new errors (verified scoped filter); frontend tsc ZERO errors (dynamic `api.adsMarketingModule[...]` indexing → explicit if/else branches — Round 25 lesson); `npm run build` GREEN (~12.2s, chunk-size warning only). Live smoke (restarted backend — killed stale :4000 listener first): dashboard 0% at_risk → key create "CRYSTALS-Kyber-1024 key pair created (256-bit security) fp_XP78C8" → channel "Athens-Brussels 381 km" → exchange "1880 bytes quantum key distilled (3.3% QBER)" → encrypt "24 B → 256 B (100% overhead)" → cert + chain verified=True leaves=1 → approval create "1/2 required" → approve "approved by 1/1" → role "john.smith@partner.com assigned role editor" — all OK. Browse-verified at 375×812 (daemon tab crashed mid-session → relaunched via `cmd /c "set BROWSE_SERVER_SCRIPT=dist/server-node.mjs && browse.exe daemon"` hidden + token re-inject on localhost origin): /mail/quantum zero overflow (scrollWidth 375 == clientWidth) zero errors, 10 cards, mobile bottom nav visible, live data renders (Kyber/Dilithium/Athens-Brussels/Chain verified/key label; readiness NOT 75% on tenant_001 because seed has 17 messages → needed=5 keys — correct); /mail/collaboration zero overflow zero errors, 15 cards, mobile nav visible (6 overflowEls are pre-existing presence/comment truncate spans — benign).
  - Plumbing: 18 delegations in AdsMarketingModuleService.ts (after `mailBillingLog` — `mailBillingOveragePolicy` passes null through); 19 routes in ads-marketing-module.ts (POST downgrade/auto-renew/cancel, GET addons + GET addons/active, POST/DELETE addons/:addonId, GET/PUT overage, POST overage/invoice, GET/POST contracts, GET contracts/status, POST contracts/cancel, GET/PUT alerts, GET credits); 17 client methods in client.ts (after `mailBillingLog:`).
  - Frontend: `MailBilling.tsx` extended — plan-card row now shows `effectiveMonthly` (contract-aware) + addon total + auto-renew off warning; 4 new cards (Add-ons storefront w/ Active chips + remove, Overage policy w/ mode select + projected cost rows + Invoice overage, Enterprise contract w/ create form + active summary + cancel, Usage alerts w/ toggle + severity list); plan row action bar (Downgrade select + auto-renew toggle + Cancel subscription); invoice rows show kind chips (addon/overage/refund) + credit amounts. 2×2 sections grid kept `min-w-0` (Round 26 overflow fix preserved). Route/nav unchanged (/mail/billing already registered).
  - Gates: full backend suite 2456/2456 (102 files); `NovaMailRound12.test.ts` 26/26 (7 describes incl. new dashboard effectiveMonthly regression); backend tsc — NO new errors; frontend tsc ZERO errors; `npm run build` GREEN (~7.7s). Live smoke (restarted backend): addon add "prorated $5.00" → dashboard addons 1 (+5/mo), overage mode bill, contract "Smoke Corp $300/yr → $25/mo effective" + nextInvoice $30, downgrade blocked "Mailboxes usage (5 / 1) exceeds the plan limit" (after fix), auto-renew off, overage invoice INV-001002 $12.00. Browse-verified at 375×812: zero overflow (1 benign truncate), err null, mobile bottom nav visible (375x60 @ y752), all 6 new sections render, Add addon via UI click → confirm auto-accepted → POST 200 → page auto-refresh → "Active: 1" chip + 5 Add buttons (dashboard payload grew 5446→6220); desktop 1366×768 zero overflow overflowEls [].
- **Round 29 (previous) - Mail Reader page** (frontend only; no backend changes): new `MailReader.tsx` at `/mail/reader` — a split-view threaded reading experience: two-pane layout on lg (`lg:grid-cols-[340px_minmax(0,1fr)]`, list + reader side-by-side) with full pane swap on mobile (reader replaces list, back chevron `lg:hidden`); list pane has folder chips w/ unread counts + client-side search (subject/sender/body) + per-row avatar initials (deterministic color), unread dot, star (hover on lg), snippet, attachment icon, paperclip; reader pane shows the full thread (all messages w/ avatars + Ani AI summary + attachments) with toolbar (Star/Archive/Trash), inline Reply (prefilled `Re:` + quoted body with `>` prefix, mailbox select, Ctrl+Enter hint) and Forward (`Fwd:` + forwarded-message block) forms — no modal, AI smart reply chips (`mailSmartReply` tones concise/friendly/professional, tap-to-fill), snooze presets (Later today/Tomorrow/Next week), J/K keyboard navigation (guarded against inputs) + Esc closes; swipe gestures on list rows (touchstart/touchmove/touchend, translateX visual w/ Archive/Trash bg layers, `<-60px` = archive, `>60px` = trash); 30s auto-refresh toggle + `n0va:refresh-data` listener + skeleton + empty state + busy-per-action. Registered in App.tsx (lazy route after /mail/calendar) + Sidebar ("Mail Reader", MailOpen icon — new import, after Mail Calendar) + CommandPalette.

- **Round 35 - Mail Automation Studio + Deliverability Center, Nurture sequences + Unsubscribe center, Team ticketing, Backup & restore + Mail API keys** (backend + frontend; `NovaMailRound17.test.ts`, 41 tests green, stable across 3 runs; full suite 2611/2611, 107 files):
  - 7 new services (all with singleton exports): `MailAutomationService` (`mailAutomation`) w/ AUTOMATION_TRIGGERS 6 (on_receive/on_send/on_star/on_label/on_snooze/manual) + AUTOMATION_ACTIONS 9 (label/archive/move/mark_read/star/forward/reply/notify/trash), createAutomation (normalizeSteps: blank step names become "Step N"), runAutomation (condition checks per step; delayHours>0 schedules into `mail_automation_runs` instead of executing), testAutomation (dry-run stepResults w/ wouldRun + actionsToRun), dueRuns (executes due scheduled steps), automationDashboard (total/enabled/totalRuns/totalMatches/scheduledPending/byTrigger/recentLog), automationLog; `MailDeliverabilityService` (`mailDeliverability`) w/ BOUNCE_CLASSES 3 (hard/soft/complaint) + SUPPRESSION_REASONS 4 (hard_bounce/complaint auto, manual/unsubscribe) + REPUTATION_LEVELS 3 (good >=70/fair >=40/poor), recordBounce (auto-suppresses hard+complaint) / recordComplaint, bounceStats (byType + hardRate), listBounces (type filter), suppressEmail (reason whitelist + duplicate guard)/unsuppressEmail/suppressionList (reason filter)/suppressionStatus, sendingReputation (deterministic base `85 + hashStr(tenantId + "|rep") % 11`, minus 5/hard + 10/complaint, min 5 max 100), listHygiene (contacts matched to hard_bounce/complaint suppressions), deliverabilityDashboard (bounces + suppression.byReason + reputation + hygiene + alerts always >=1 on seed); `MailSequenceService` (`mailSequence`) w/ createSequence (steps w/ delayHours) / listSequences (raw array) / enrollContact (nextRunAt = now + first step delay) / enrollMany / pause+resume+unenroll / advanceSequence (due active enrollments only; sends via `mailMessage.composeSend`; sequence not active -> enrollment paused; no more steps -> completed) / sequenceProgress (byStatus + reached per step) / sequencesDashboard; `MailUnsubscribeService` (`mailUnsubscribe`) w/ generateLink (`https://n0va.io/unsubscribe/<token>?cat=` from `mail_unsubscribe_links`) / unsubscribe (prefs.unsubscribed + log `mail_unsubscribes`) / preferences get+update (categories array) / unsubscribeStatus / unsubscribeLog / unsubscribeDashboard (byReason + byCategory + subscribed/unsubscribed counts); `MailTicketService` (`mailTicket`) w/ createTicket (slaDeadline = now + slaHours, DEFAULT_SLA_HOURS 24) / listTickets (status/priority/assignee/tag/search filters) / update / assign / priority / tag + untag (dedupes) / notes / resolve / reopen / escalate (priority -> urgent + fresh SLA) / slaOverview ({active, byStatus overdue|due_soon|healthy|done, overdue, dueSoon, healthy, summary}) / ticketDashboard (byStatus/byPriority/byAssignee + sla + avgResolutionHours) / ticketLog + ticketEvents; `MailBackupService` (`mailBackup`) w/ createBackup (snapshot of mailboxes/messages/contacts/threads/rules/automations/sequences/templates/campaigns/tickets + sizeBytes via estimateMessageBytes + 10-newest cap) / backups ({backups, total}) / restore (stamps restoredAt + restoredCount + backup log) / delete / backupSchedule GET+PUT (autoBackup/intervalHours/retentionDays) / backupsDashboard (total + totalBytes + recentRestores + schedule + summary); `MailApiKeyService` (`mailApiKey`) w/ createApiKey (`n0va_mk_` prefix + hashed key stored + prefix/last4 exposed; scopes must include at least one of send/read/webhook/campaigns else throws) / keys ({keys, total}, NO raw key field) / get / revoke (status revoked) / verifyApiKey (valid + reason revoked/invalid) / recordApiCall (increments per-key + inserts `mail_api_key_usage`) / usage (byAction + recent) / apiKeyDashboard (keys + active + callsToday + callsTotal + byAction + summary). New collections: `mail_automations`, `mail_automation_log`, `mail_automation_runs`, `mail_bounces`, `mail_suppression`, `mail_preferences`, `mail_unsubscribes`, `mail_unsubscribe_links`, `mail_sequences`, `mail_sequence_enrollments`, `mail_tickets`, `mail_ticket_events`, `mail_backups`, `mail_backup_policy`, `mail_api_keys`, `mail_api_key_usage`. Real bug fixed: `MemoryStore.delete` removes only FIRST match -> `deleteSequence` uses a while-loop (guard < 1000) draining `mail_sequence_enrollments`; returns `enrollmentsRemoved`.
  - Plumbing: 7 imports + ~88 delegations at the END of AdsMarketingModuleService.ts (after the mailCollab2 block); ~77 routes before `export default router` (static paths BEFORE param routes: `/mail/automations/dashboard|triggers|actions|due|log`, `/mail/automations/:automationId/run|test|toggle`, `/mail/deliverability/dashboard|bounce-stats|bounces[+POST]|complaints|suppression[+POST]|suppression/:email|suppression/status|reputation|hygiene`, `/mail/sequences/dashboard|advance`, `/mail/sequences/:sequenceId/enroll|enroll-many|toggle|progress`, `/mail/enrollments/:enrollmentId/pause|resume|unenroll`, `/mail/unsubscribe/link|status|log|dashboard`, `/mail/unsubscribe[POST]`, `/mail/preferences/:email[GET|PUT]`, `/mail/tickets` GET+POST, `/mail/tickets/:ticketId` GET+PUT, `/mail/tickets/:ticketId/assign|priority|tags|notes|resolve|reopen|escalate|events`, `/mail/tickets/sla|dashboard|log`, `/mail/backups` GET+POST, `/mail/backups/:backupId|restore`, `/mail/backups/schedule` GET+PUT, `/mail/backups/dashboard`, `/mail/api-keys` GET+POST, `/mail/api-keys/verify|dashboard` [STATIC BEFORE :apiKeyId], `/mail/api-keys/:apiKeyId|revoke|usage`); ~90 client methods in client.ts (after `mailCollab2Dashboard` - `mailAutomations` through `mailApiKeyDashboard`). NOTE: `mailApiKeys` rows have NO `key` field; `mailUnsubscribeLink` returns `{token, link, email, summary}`; suppression list rows are `{suppressionId, email, reason, source, ...}`; `mailBackups` rows `{backupId, label, sizeBytes, snapshot, createdAt, restoredAt}`; sequence rows are raw (use `_id`, no `sequenceId` key).
  - Frontend: 5 new pages - `MailAutomations.tsx` (/mail/automations; 4 stat cards, trigger chips w/ per-trigger counts, automation cards w/ steps + conditions + delay chips + run/test/toggle/delete, run-against-message modal (message select from `mailMessages({folder:inbox})`), test modal w/ stepResults wouldRun list, multi-step create modal w/ action builder), `MailDeliverability.tsx` (/mail/deliverability; reputation bar + level chip, alerts, bounce log w/ type filter chips + record-bounce modal, suppression list w/ undo + reason filter + suppress modal, list hygiene chips), `MailSequences.tsx` (/mail/sequences; 4 stat cards, sequence cards w/ steps + +Nh chips + reached counts + advance/enroll/toggle/delete, pipeline byStatus grid, unsubscribe center w/ link generator + preferences lookup/save (category chips) + opt-out log, create modal w/ multi-step editor, enroll modal w/ comma-separated emails), `MailTickets.tsx` (/mail/tickets; 4 stat cards (open/overdue/urgent/avgResolution), status + priority filter chips, search, ticket rows w/ SLA badges (client-side slaOf), file modal (subject/from/assignee/priority/SLA hours), detail modal w/ resolve/reopen/escalate/priority/assignee/tags/notes, SLA health chips, activity log), `MailAdmin.tsx` (/mail/admin; 3 backup stat cards, backups list w/ restore/delete, schedule form (autoBackup/intervalHours/retentionDays + Save), Back up now modal w/ label + reveal toast, API keys section w/ dashboard counts + byAction chips, key rows w/ scopes + usage/revoke, create modal w/ scope chips + one-time key reveal + copy, verify form w/ valid/reason result, per-key usage panel). All registered in App.tsx (lazy, after /mail/threads) + Sidebar (Workflow/Gauge/ListOrdered/Ticket/KeyRound icons, after Mail Threads) + CommandPalette; all w/ `n0va:refresh-data` listener + 30s auto-refresh toggle + skeleton + busy-per-action.
  - Gates: full backend suite 2611/2611 (107 files); `NovaMailRound17.test.ts` 41/41 stable 3x; backend tsc - no NEW errors; frontend tsc ZERO errors; `npm run build` GREEN (~59s). Live smoke (restarted backend - kill stale :4000 listener first; NOTE be35.cmd launcher needs `%%20` for the tsx loader file URL in batch files): automation create + run + test (1/1 would run), hard bounce -> auto-suppressed (1 suppressed 1 bounce), sequence create + enroll 2/2 + advance (by design no due steps on 24h first delay) + unsubscribe link + prefs, ticket create/note/escalate/resolve + SLA 0 active, backup create (17 messages) + list + restore + schedule every 12h, api-key create (`n0va_mk_`) + verify valid send+read + usage 0 + revoke + verify invalid reason=revoked - all OK. Browse-verified at 375x812 (one-shot daemon pattern): all 5 pages zero doc overflow (scrollWidth 375 == clientWidth) zero errors, mobile bottom nav visible (60x375 fixed nav), live data renders (sequence cards w/ 2 enrollments, ticket rows w/ SLA chips, backup list + schedule 12/14), File-ticket + Back-up-now modals open via UI clicks; full create-backup UI flow verified (fill label -> Create backup -> toast "Backup created ... captured 17 message(s)" -> modal closes -> list refreshes); desktop 1366x768 automations/tickets/admin zero overflow overflowEls [].

### Next

1. Round 35 items done: Mail Automation Studio + Deliverability Center, Nurture sequences + Unsubscribe center, Team ticketing, Backup & restore + Mail API keys live-verified via API + UI (automation create/run/test, bounce auto-suppress, sequence enroll/link/prefs, ticket lifecycle + SLA, backup create/restore/schedule, api-key create/verify/revoke), 5 pages browse-verified at 375x812 + 1366x768 zero overflow zero errors (mobile nav 60x375 visible, data renders, modals open, create-backup flow through the UI incl. toast), AGENTS.md updated (full suite 2611/2611, 107 files).
2. Round 36 items done: Mail Biometrics, Zero Trust, AI Governance, Performance & Chaos Engineering — 5 backend services (NovaMailRound18.test.ts 35 green ×4, full suite 2646/2646, 108 files) + 5 frontend pages (MailBiometrics/MailTrust/MailGovernance/MailPerformance/MailChaos) registered in App.tsx + Sidebar + CommandPalette, tsc ZERO errors, `npm run build` GREEN (~8.4s), browse-verified 375×812 zero overflow zero errors (benign truncate spans only; chaos Run clicked through the UI → resilience 100/100), AGENTS.md updated.
3. Round 37 items done: Mail Focus & Zero-Click Intelligence — Day-to-Day Execution Guide gaps (§6.2 Focus Mode, §6.1 Notification tiers, §6.3 Smart Batching, §4.3 Batch Intelligence, §12.2 Daily Friction Score, §12.3 ROI Calculator) as 1 new service (`MailFocusService` `mailFocus`: FOCUS_MODES 5 incl. open-ended vacation, NOTIFICATION_TIERS 4, BATCH_SUGGESTIONS 6; activateFocus w/ re-activation guard + duration validation, focusStatus w/ per-day stats + remainingMin, endFocus w/ deterministic emailsHandled/timeSavedMin/focusScore seeded on `tenantId|mode|suffix` — NOT sessionId (genId embeds Date.now, Round 19 lesson), extendFocus, notificationTiers inbox classification, smartBatch time/focus-aware policy, batchIntelligence grouping + executeBatchSuggestions w/ folder/label side effects, frictionScore w/ baseline 3460 + reductionPct + level, roiCalculator w/ annualValue = hoursSavedYear×rate×users (ROI 12791% @1×$75), focusLog/focusDashboard + `suggestions` in dashboard; collections `mail_focus_sessions`, `mail_focus_log`, `mail_friction`) + 17 delegations + 16 routes (`/mail/focus/*` — static paths before `:sessionId`; sessions/:sessionId/end|extend after /status) + 17 client methods + `NovaMailRound19.test.ts` 24 tests green stable 3× (full suite 2670/2670 across 109 files; fixtures: tenants nova-mail19/nova-mail19b/nova-mail19c — batch fixtures isolated on T3 so tier counts stay stable) + frontend page `MailFocus.tsx` at /mail/focus (activate form w/ mode select + custom minutes + behavior hint, active-session card w/ +30 min/End focus, 4 stat cards, tier grid T1-T4 w/ delivery + examples + counts, smart batching policy chips, batch intelligence catalog (spec §4.3), friction score w/ baseline bar + input recompute, ROI calculator w/ team size + hourly rate, recent activity log, 30s auto-refresh + `n0va:refresh-data` listener) registered in App.tsx (after /mail/chaos) + Sidebar ("Focus Time", Target icon — already imported) + CommandPalette + MailInbox pull-to-refresh polish (touch handlers on root div, `lg:hidden` indicator strip, 60px threshold → loadAll, Release-to-refresh state) — tsc ZERO errors both sides, `npm run build` GREEN (~41s), backend tsc NO new errors, browse-verified 375×812 (sw 375 == cw, overflowEls [], err null, mobile nav visible, Activate click → "Deep Work focus active · 120 min left", End click → server-side ended) + 1366×768 clean, AGENTS.md updated.

### Next

1. Round 38 items done: Neural Email Composer (§3.3 Smart Compose zero-tap drafting + roadmap Q3-2026 "In Development") — 1 new service (`MailComposerService` `mailComposer`: COMPOSER_MODES 3 / COMPOSER_TONES 3 / COMPOSER_INTENTS 6, composerCatalog, composeDraft w/ deterministic full-body generation (greeting/✅⚠️ section verdicts/intent opener/CTA/sign-off/basedOn/confidence 70-99, actions send|edit|regenerate|don't_like, clicksToSend 1), regenerateDraft variant+1, dislikeDraft w/ feedback + auto-regenerate, saveEdits, sendDraft 1-click via mailMessage.composeSend + status sent, listDrafts/getDraft/deleteDraft, composerDashboard/composerLog; collections mail_composer_drafts, mail_composer_log; determinism seed `${from.email}|${subject}|${mode}|${tone}|${variant}`) + 11 delegations + 11 routes + 12 client methods + `NovaMailRound20.test.ts` 19 green ×3 (full suite 2689/2689, 110 files) + page `MailComposer.tsx` at /mail/composer (4 stat cards, message-select zero-tap compose form, draft preview w/ Send(1-click)/Regenerate/Edit/Don't-like, drafts list w/ status chips) registered in App.tsx + Sidebar ("Mail Composer", PenLine) + CommandPalette — tsc ZERO errors both sides, build GREEN (~14.8s), browse-verified 375×812 + 1366×768 zero overflow zero errors (draft written + Send clicked through the UI → sent folder), AGENTS.md updated.
2. N0VA MAIL polish (unchanged): recharts tooltip overflow on /campaigns (benign); real-device emulation spot-check; MailCampaigns recipient-count tooltips - OR continue backend (any unclaimed spec sections — §13.3 Data Residency, §13.4 White-Label, §13.5 Marketplace, §11 SDK/Dev metrics remain). User picks scope.

## Relevant Files

- 
0va\packages\backend\src\services\MailAutomationService.ts (mailAutomation) — Round 35 service (AUTOMATION_TRIGGERS 6 / AUTOMATION_ACTIONS 9, createAutomation w/ normalizeSteps, runAutomation/testAutomation w/ stepResults, dueRuns, automationDashboard/automationLog; collections mail_automations, mail_automation_log, mail_automation_runs); 
0va\packages\backend\src\services\MailDeliverabilityService.ts (mailDeliverability) — Round 35 service (BOUNCE_CLASSES 3 / SUPPRESSION_REASONS 4 / REPUTATION_LEVELS 3, recordBounce w/ auto-suppress, suppressionList/status, sendingReputation deterministic base 85+hash%11, listHygiene, deliverabilityDashboard; collections mail_bounces, mail_suppression)
- 
0va\packages\backend\src\services\MailSequenceService.ts (mailSequence) — Round 35 service (createSequence, enrollContact/enrollMany/pause/resume/unenroll, advanceSequence w/ composeSend, sequenceProgress, sequencesDashboard; collections mail_sequences, mail_sequence_enrollments; deleteSequence while-loop drain of enrollments — MemoryStore.delete first-match-only bug); 
0va\packages\backend\src\services\MailUnsubscribeService.ts (mailUnsubscribe) — Round 35 service (generateLink/unsubscribe/preferences/unsubscribeStatus/log/dashboard; collections mail_preferences, mail_unsubscribes, mail_unsubscribe_links)
- 
0va\packages\backend\src\services\MailTicketService.ts (mailTicket) — Round 35 service (createTicket w/ slaDeadline, listTickets filters, assign/priority/tag/untag/notes, resolve/reopen/escalate, slaOverview, ticketDashboard, ticketLog/events; collections mail_tickets, mail_ticket_events); 
0va\packages\backend\src\services\MailBackupService.ts (mailBackup) — Round 35 service (createBackup snapshot + 10-newest cap, restore/delete, backupSchedule GET/PUT, backupsDashboard; collections mail_backups, mail_backup_policy); 
0va\packages\backend\src\services\MailApiKeyService.ts (mailApiKey) — Round 35 service (createApiKey 
0va_mk_ hashed, keys/get/revoke, verifyApiKey, recordApiCall/usage, apiKeyDashboard; collections mail_api_keys, mail_api_key_usage)
- 
0va\packages\backend\src\__tests__\NovaMailRound17.test.ts — Round 35, 41 tests all passing (deterministic; tenant 
ova-mail17 with MB17/MSG17/CONTACT17 fixtures; 7 describes: automations / deliverability / sequences+unsubscribe / tickets / backups / api-keys / dashboards; fixes during dev: reputation base pinning, untag dedupe, list shape unwraps)
- 
0va\packages\frontend\src\pages\MailAutomations.tsx, MailDeliverability.tsx, MailSequences.tsx, MailTickets.tsx, MailAdmin.tsx — Round 35 pages (routes /mail/automations, /mail/deliverability, /mail/sequences, /mail/tickets, /mail/admin; all registered in App.tsx after /mail/threads + Sidebar Workflow/Gauge/ListOrdered/Ticket/KeyRound icons + CommandPalette)
- `packages/frontend/src/components/HelpDialog.tsx` — Round 11 contextual help (fixes/shortcuts/voice tabs, quickFixes/applyQuickFix/fixAll)
- `packages/frontend/src/components/Layout.tsx` — Round 11 keyboard shortcuts (§14.2) + responsive topbar + `hidden lg:block` sidebar (temp ErrBoundary removed after Round 14 verification)
- `packages/frontend/src/components/MobileNav.tsx` — Round 11 mobile bottom nav (§11)
- `packages/frontend/src/components/AIAssistant.tsx` — `n0va:toggle-assistant` listener + mobile positioning; `packages/frontend/src/components/QuickActions.tsx` — mobile FAB offset; `packages/frontend/src/pages/CommandCenter.tsx` — `n0va:refresh-data` listener (also Round 10 page)
- `packages/frontend/src/pages/Segmentation.tsx` — Round 14 `normSeg` normalizer (`groups||[]`, `estimatedSize ?? size ?? 0`, `description||""`); `packages/frontend/src/pages/LandingPages.tsx` — Round 14 `normLp` normalizer (`name←title`, `views/conversions: 0`, `tags: []`, `url: "/"+slug`) — both browse-verified clean at 375×812
- Round 14 envelope-fix files (`Array.isArray(r) ? r : r?.data || []` in loaders): Audiences.tsx, Approvals.tsx, Agents.tsx, Creatives.tsx, CreativeGallery.tsx, NotificationCenter.tsx, MentionsPage.tsx, AudienceInsights.tsx, KeywordManager.tsx, Recipes.tsx, CustomDashboards.tsx, ReportCenter.tsx, WebhookDetail.tsx, AgentCreator.tsx, Automation.tsx, CampaignAlerts.tsx, CampaignDetail.tsx, CampaignWizard.tsx, Platforms.tsx, ReportBuilder.tsx, CampaignIssues.tsx, Dashboard.tsx, WebhooksPage.tsx (list + deliveries)
- `packages/backend/src/services/AdsMarketingModuleService.ts` — delegations for all rounds (~3450+ lines, imports for all round services)
- `packages/backend/src/routes/ads-marketing-module.ts` — ~80 routes across all rounds
- `packages/frontend/src/api/client.ts` — matching ads-marketing API methods; pre-existing TS1117 duplicates (do not touch unless making build green)
- `packages/backend/src/services/BrandSafetyGuardianService.ts`, `AttributionReportService.ts`, `CrossPlatformPerformanceService.ts` — round 9, all tested
- `packages/backend/src/services/CampaignLaunchWizardService.ts`, `CreativeAutoRefreshService.ts`, `QuickFixService.ts`, `CrossModuleWorkflowService.ts` — round 8, all tested
- `packages/backend/src/services/CampaignAudienceBuilderService.ts`, `BudgetAutopilotService.ts`, `WeeklyMonthlyRoutinesService.ts` — round 7, all tested
- `packages/backend/src/services/CampaignAutoApproveService.ts`, `CampaignTriageService.ts`, `CampaignTemplateService.ts`, `CommandCenterService.ts` — round 6, all tested
- `packages/backend/src/__tests__/SilentGuardian.test.ts` (11), `OneClickExecutionPart2.test.ts` (15), `SetAndForget.test.ts` (17), `OneClickExecution.test.ts` (15), `DailyExecutionDashboard.test.ts` (3), `BatchActions.test.ts` (6), `PortfolioQuickViews.test.ts` (5)
- `packages/backend/src/services/CampaignIssueService.ts` — Round 12: added `campaignIssueService` singleton export; `packages/backend/src/services/CampaignOptimizerService.ts` — Round 12: added `campaignOptimizerService` singleton export
- `packages/backend/src/__tests__/DeeperMarketingServices.test.ts` — was 22 failures (fixed Round 12 via singleton exports), now 104/104 passing
- `packages/backend/src/services/MailboxService.ts`, `MailMessageService.ts`, `MailRulesService.ts`, `MailSearchService.ts`, `MailAIService.ts` — Round 15 N0VA MAIL core services (singleton exports `mailboxService`, `mailMessage`, `mailRules`, `mailSearch`, `mailAI`)
- `packages/backend/src/services/MemoryStore.ts` — Round 15 mail seed data for `tenant_001` (mailboxes/folders/contacts/messages/rules) with `insertedMailboxes` array fix
- `packages/backend/src/__tests__/NovaMailRound1.test.ts` — Round 15, 37 tests all passing
- `packages/backend/src/services/WorkflowBuilderService.ts` — Round 15 flake fix: `saveWorkflow` id is `wf_${Date.now()}_${random6}` (was `wf_${Date.now()}` only)
- `packages/frontend/src/pages/MailInbox.tsx`, `MailSearch.tsx`, `MailRules.tsx`, `Mailboxes.tsx`, `MailAI.tsx` — Round 16 N0VA MAIL frontend (routes /mail, /mail/search, /mail/rules, /mail/mailboxes, /mail/ai in App.tsx + Sidebar + CommandPalette)
- `packages/backend/src/services/MailContactService.ts`, `MailAgentService.ts`, `MailComplianceService.ts`, `MailVoiceService.ts` — Round 17 services (singleton exports `mailContact`, `mailAgent`, `mailCompliance`, `mailVoice`)
- `packages/backend/src/__tests__/NovaMailRound2.test.ts` — Round 17, 38 tests all passing
- `packages/frontend/src/pages/MailContacts.tsx`, `MailAgent.tsx`, `MailCompliance.tsx` — Round 17 pages (routes /mail/contacts, /mail/agent, /mail/compliance)
- `packages/backend/src/services/MailTemplateService.ts`, `MailSignatureService.ts`, `MailSpamService.ts`, `MailFollowUpService.ts`, `MailAnalyticsService.ts`, `MailAttachmentService.ts` — Round 18 services (singleton exports `mailTemplates`, `mailSignature`, `mailSpam`, `mailFollowUp`, `mailAnalytics`, `mailAttachment`)
- `packages/backend/src/__tests__/NovaMailRound3.test.ts` — Round 18, 46 tests all passing
- `packages/backend/src/services/MailCollaborationService.ts` (`mailCollab`), `MailPredictiveService.ts` (`mailPredict`), `MailCampaignService.ts` (`mailCampaign`), `MailDiscoveryService.ts` (`mailDiscovery`), `MailDomainService.ts` (`mailDomain`) — Round 19 services (collab, predict, campaigns, discovery, domains)
- `packages/backend/src/__tests__/NovaMailRound4.test.ts` — Round 19, 52 tests all passing (deterministic; run 5-8× to verify — see flake fix note)
- `packages/frontend/src/pages/MailCollaboration.tsx`, `MailPredict.tsx`, `MailCampaigns.tsx`, `MailDiscovery.tsx`, `MailDomains.tsx` — Round 19 pages (routes /mail/collaboration, /mail/predict, /mail/campaigns, /mail/discovery, /mail/domains in App.tsx + Sidebar + CommandPalette)
- `packages/backend/src/services/MailVoiceNoteService.ts` (`mailVoiceNote`), `MailMultimodalService.ts` (`mailMultimodal`), `MailNeuralService.ts` (`mailNeural`) — Round 20 services (voice notes, multimodal content, neural mailbox)
- `packages/backend/src/__tests__/NovaMailRound5.test.ts` — Round 20, 48 tests all passing (deterministic; verified 5×)
- `packages/frontend/src/pages/MailVoice.tsx` — Round 20 page (route /mail/voice; also MailRules.tsx AI-rule modal + MailAI.tsx neural section)
- `packages/frontend/src/pages/MailTemplates.tsx`, `MailSignatures.tsx`, `MailSpam.tsx`, `MailFollowUps.tsx`, `MailAnalytics.tsx`, `MailFiles.tsx` — Round 18 pages (routes /mail/templates, /mail/signatures, /mail/spam, /mail/followups, /mail/analytics, /mail/files)
- `packages/backend/src/services/MailCommandCenterService.ts` (`mailCommand`), `packages/backend/src/services/MailRulesService.ts` (`sweepRules`), `packages/backend/src/services/MailAgentService.ts` (`sendScheduleNow`) — Round 21 services (daily execution dashboard + one-click actions; route /mail/command-center, POST /mail/rules/sweep, POST /mail/schedules/:scheduleId/send-now)
- `packages/backend/src/__tests__/NovaMailRound6.test.ts` — Round 21, 23 tests all passing (deterministic; sweep counts only ENABLED rules; script rules evaluated per-rule since `runAllScriptRules` has no results array; `receiveMessage` ignores a passed `receivedAt`)
- `packages/frontend/src/pages/MailCommandCenter.tsx` — Round 21 page (route /mail/command-center; verdict card, morning report, 4 stat cards, one-click action bar, ready-actions footer, 6-section grid, 30s auto-refresh toggle + `n0va:refresh-data` listener)
- `packages/frontend/src/pages/MailInbox.tsx` — Round 21 batch selection mode (`selected` array + `mailBatchOps` toolbar: Read/Unread/Star/Unstar/Archive/Trash/Delete-in-trash/Label-apply/Clear; select-all checkbox in list header); `packages/frontend/src/pages/MailRules.tsx` — Round 21 Run-now button (`sweepRules` handler, Play icon, `hidden sm:inline` label); `packages/frontend/src/components/Sidebar.tsx` — Round 21 mail badge refresh on route change (`useLocation`) + `n0va:refresh-data` listener
- `packages/backend/src/services/MailOpsService.ts` (`mailOps`) — Round 22 service (ops dashboard, P0-P4 incident lifecycle, retry/checkpoint/threat-rule/housekeeping one-clicks, ops log); `packages/backend/src/services/MailStorageService.ts` (`mailStorage`) — Round 22 service (hot/warm/cool/cold tiering, policy, AI cleanup suggestions, storage forecast)
- `packages/backend/src/services/MailSearchOperatorsService.ts` (`mailSearchOp`) — Round 23 service (§3.4.2, 15-operator query language: parseQuery/operatorSearch/operatorReference/searchExamples/recentQueries/clearHistory/operatorStats); `packages/backend/src/services/MailAbuseService.ts` (`mailAbuse`) — Round 23 service (§2.4, five-layer anti-abuse: scanIncoming/rateLimitCheck/becScan/dlpScan/impossibleTravel/sendingPatternAnomaly/honeypotStatus/threatResponse/abuseLog/abuseOverview/abuseDashboard)
- `packages/backend/src/__tests__/NovaMailRound8.test.ts` — Round 23, 37 tests all passing (deterministic; tenant `nova-mail8` with burst/calm/abuse mailboxes; outcomes pinned empirically via tsx eval before writing assertions)
- `packages/frontend/src/pages/MailSearch.tsx` — Round 23 operator panel (reference chips, examples, recent+clear, parse explanation/invalid chips, scored results); `packages/frontend/src/pages/MailProtection.tsx` — Round 23 page (route /mail/protection; verdict + 5 stats + 5-layer display + one-click defense actions + honeypots + events log; registered in App.tsx after /mail/ops + Sidebar "Mail Protection" + CommandPalette)
- `packages/backend/src/__tests__/NovaMailRound7.test.ts` — Round 22, 29 tests all passing (deterministic; tenant `nova-mail7` with `r7_nl`/`r7_big`/`r7_failed`/`r7_frozen`/duplicate-thread fixtures; shared `mail_ops_log` + `mail_storage_policy` collections)
- `packages/frontend/src/pages/MailOps.tsx` — Round 22 page (route /mail/ops; health cards, one-click ops bar, alerts, incidents w/ File modal, tier bars + policy form, AI cleanup suggestions, ops log; registered in App.tsx after /mail/command-center + Sidebar "Mail Ops" Activity icon + CommandPalette)
- `packages/backend/src/services/MailWebhookService.ts` (`mailWebhook`) — Round 24 service (§4.4, 11-event webhook registry: createWebhook/list/update/delete/pingWebhook/fireEvent/deliveries/deliveryStats/webhookOverview/webhookLog; HMAC-signed deterministic deliveries); `packages/backend/src/services/MailAgentRegistryService.ts` (`mailAgentRegistry`) — Round 24 service (§11, agent framework: 10 personas/25 tools, registerAgent/discoverTools/sessions/agentAction/hitlQueue/resolveHitl/agentDashboard/agentLog)
- `packages/backend/src/services/MailSearchOperatorsService.ts` — Round 24 extended to 20 operators (`has:voice`/`has:poll`/`collaborated:with:`/`ai:suggested`/`visual:contains:`; stale `has`-handler line removed; `operatorStats` rows include `example`); `packages/backend/src/__tests__/NovaMailRound8.test.ts` — Round 23→24, 15→20 assertions updated
- `packages/backend/src/__tests__/NovaMailRound9.test.ts` — Round 24, 45 tests all passing (deterministic; tenant `nova-mail9`; webhook delivery outcomes keyed on `hashStr(hook._id|event|joinedEvents) % 7`)
- `packages/frontend/src/pages/MailWebhooks.tsx` — Round 24 page (route /mail/webhooks; stats, subscriptions w/ secret reveal + ping/delete, create modal, deliveries + stats); `packages/frontend/src/pages/MailAgents.tsx` — Round 24 page (route /mail/agents; persona grid, tool reference, agent cards w/ API key copy, register modal, session panel, HITL queue, audit log) — both registered in App.tsx after /mail/protection + Sidebar ("Webhooks"/"Mail Agents") + CommandPalette
- `n0va\packages\backend\src\services\MailIntegrationService.ts` (`mailIntegration`) — Round 25 service (§9; CONNECTORS 10/INTEGRATION_CATEGORIES 6/BRIDGE_EVENTS 7; connect w/ reconnect semantics, authorize/refresh/update/disconnect, syncNow/syncHistory/syncStatus, 9 provider actions, bridges CRUD + trigger, integrationLog/Alerts/Overview/Dashboard; collections `mail_connections`, `mail_integration_jobs`, `mail_integration_log`, `mail_integration_bridges`; NOTE: `toPublic()` includes `connectionId` — never spread a duplicate key)
- `n0va\packages\backend\src\__tests__\NovaMailRound10.test.ts` — Round 25, 37 tests all passing (deterministic; tenant `nova-mail10`; sync outcome hashStr(${connectionId}|sync) % 11 === 0 → failed else success; 4x TS2783 + 8x TS18048 fixed in tsc)
- `n0va\packages\frontend\src\pages\MailIntegrations.tsx` — Round 25 page (route /mail/integrations; 5 stat cards, alerts banner, 10-connector grid w/ per-connection sub-cards, bridges automation list, connect + bridge modals, recent syncs + integration log, 30s auto-refresh toggle; registered in App.tsx after /mail/agents + Sidebar "Mail Integrations" Plug icon + CommandPalette)
- 
0va\packages\backend\src\services\MailBillingService.ts — Round 26 service (mailBilling; §3.3 4-plan billing: PLAN_ORDER/9 limit dimensions, usage metering, upgrade w/ proration + open invoices, payment methods, forecast, billingDashboard; collections mail_billing, mail_invoices, mail_payment_methods, mail_billing_log); 
0va\packages\backend\src\services\MailNotificationService.ts — Round 26 service (mailNotifications; 12-type center w/ dedupe + settings, 11-source collectAlerts scanner; collections mail_notifications, mail_notification_settings)
- 
0va\packages\backend\src\__tests__\NovaMailRound11.test.ts — Round 26, 33 tests all passing (deterministic; unique tenants 
ova-mail11_{n} since MemoryStore has NO clear(); invoice card min-w-0 grid fix regression-verified via browse)
- 
0va\packages\frontend\src\pages\MailBilling.tsx — Round 26+30 page (route /mail/billing; Round 26: plan cards, usage bars, forecast, payment methods, invoices w/ Pay, activity log, Add card modal; Round 30: + Add-ons storefront, Overage policy, Enterprise contract, Usage alerts cards, Downgrade/auto-renew/cancel action bar, invoice kind chips + credits — min-w-0 grid fix preserved); 
0va\packages\frontend\src\pages\MailNotifications.tsx — Round 26 page (route /mail/notifications; stat cards, type filter chips, list w/ severity dots + links, per-type settings, Scan for alerts / Mark all read / Clear all) — both registered in App.tsx after /mail/integrations + Sidebar (Mail Billing CreditCard, Mail Alerts Bell) + CommandPalette
- 
0va\packages\backend\src\__tests__\NovaMailRound12.test.ts — Round 30, 26 tests all passing (deterministic; unique tenants 
ova-mail12_{n}; add-ons/overage/contracts/alerts/downgrade/auto-renew + dashboard effectiveMonthly regression)
- 
0va\packages\frontend\src\pages\MailDispatch.tsx — Round 27 page (route /mail/dispatch; queued sends w/ create + send-now + cancel, follow-up reminders w/ done/delete, campaign send windows w/ launch/approve/reject/pause/resume, 4 stat cards, 30s auto-refresh + 
0va:refresh-data listener; registered in App.tsx after /mail/command-center + Sidebar "Mail Dispatch" SendHorizonal + CommandPalette; NO backend changes — uses existing mailListScheduled/mailScheduleSend/mailSendScheduleNow/mailCancelSchedule/mailFollowUps/mailCompleteFollowUp/mailDeleteFollowUp/mailCampaigns/mailLaunchCampaign/mailApproveCampaign/mailRejectCampaign/mailPauseCampaign/mailResumeCampaign)
- 
0va\packages\frontend\src\pages\MailCalendar.tsx — Round 28 page (route /mail/calendar; 42-cell monthly grid + legend + day-detail panel, per-item quick actions, month nav; registered in App.tsx after /mail/dispatch + Sidebar "Mail Calendar" CalendarClock + CommandPalette; NO backend changes — uses existing mailListScheduled/mailFollowUps/mailCampaigns/mailListSnoozed/mailSendScheduleNow/mailCancelSchedule/mailCompleteFollowUp/mailDeleteFollowUp/mailApproveCampaign/mailRejectCampaign/mailPauseCampaign/mailResumeCampaign/mailUnsnooze)
- 
0va\packages\frontend\src\pages\MailReader.tsx — Round 29 page (route /mail/reader; split-view threaded reader — list pane w/ folder chips + search + avatars + swipe gestures, reader pane w/ thread + inline Reply/Forward forms + AI smart reply chips + snooze presets + J/K nav; registered in App.tsx after /mail/calendar + Sidebar "Mail Reader" MailOpen + CommandPalette; NO backend changes — uses existing mailMessages/mailThread/mailSend/mailMarkRead/mailToggleStar/mailArchive/mailTrash/mailSmartReply/mailSnooze/mailUnreadSummary/mailMailboxes)
- `n0va\packages\backend\src\services\MailDiscoveryService.ts` — Round 31 extended (`mailDiscovery`; CONCEPT_TOPICS 7 / PRIVILEGE_TYPES 4 / conceptSearch / markMessagePrivileged / listPrivileges / removePrivilege / privilegeSummary / exportAuditChain / createExport chain-stamped w/ kind "ediscovery" / discoverySummary extended); `n0va\packages\backend\src\services\MailComplianceService.ts` — Round 31 extended (`mailCompliance`; placeHold + startDate/endDate, legalHoldCalendar 42-cell, REPORTS GDPR/CCPA/HIPAA + frameworkChecks + complianceReport / complianceReports / exportComplianceReport joining the export chain; collection `mail_privileges`)
- `n0va\packages\backend\src\__tests__\NovaMailRound13.test.ts` — Round 31, 19 tests all passing (deterministic; tenants `nova-mail13_{N}` w/ direct-insert fixtures + explicit `_id`s; 5 describes: concept search / privilege lifecycle / export audit chain / legal hold calendar / compliance reports)
- `n0va\packages\frontend\src\pages\MailDiscovery.tsx` — Round 31 extended (concept search clusters, privilege log w/ assert modal + remove, export audit chain card; loadAll fetches privileges + chain; NOTE: privilege list must use `mailDiscoveryPrivileges()` NOT privilegeSummary — byType counts only); `n0va\packages\frontend\src\pages\MailCompliance.tsx` — Round 31 extended (legal hold calendar w/ shiftMonth nav + today ring + event dots, compliance report packs + detail modal w/ export, hold modal start/end dates; calendar cells have NO hasEvents/isToday — compute `(c.events||[]).length` + `c.date === todayStr`; report rows use `label`, checks use `name`, status values pass/warn/fail)
- `n0va\packages\backend\src\services\MailQuantumService.ts` (`mailQuantum`) — Round 32 service (PQC_ALGORITHMS 6 / QKD_CHANNELS 5; createKeyPair/listKeys/getKey/rotateKey/revokeKey, createQkdChannel/simulateQkdExchange/qkdExchanges, encryptMessage/decryptMessage, issueCertificate/listCertificates/getCertificate/revokeCertificate/renewCertificate, certificateChain, quantumOverview/quantumDashboard/quantumLog; collections `mail_quantum_keys`, `mail_qkd_channels`, `mail_qkd_exchanges`, `mail_quantum_messages`, `mail_pqc_certs`); `n0va\packages\backend\src\services\MailCollabV2Service.ts` (`mailCollab2`) — Round 32 service (TEAM_ROLES 4 / DELEGATION_PERMISSIONS / MENTION_CONTEXTS; approvals lifecycle + dashboard, inbox delegation w/ accept/revoke + summary, team roles + matrix, mentions detect/create/list/read + summary, collab2Dashboard; collections `mail_approvals`, `mail_delegations`, `mail_team_roles`, `mail_mentions`)
- `n0va\packages\backend\src\__tests__\NovaMailRound14.test.ts` — Round 32, 31 tests all passing (deterministic; tenants `nova-mail14_{N}`; hashStr replicated in test to pin values; 6 describes: PQC keys / QKD + exchange / encryption + certs + chain / approvals / delegation-roles / mentions-dashboard)
- `n0va\packages\frontend\src\pages\MailQuantum.tsx` — Round 32 new page (route /mail/quantum; readiness gauge + level, 4 stat cards, layer bars, algorithm catalog, key vault, QKD channels + exchanges, cert store + chain, quantum log, 3 modals; registered in App.tsx after /mail/notifications + Sidebar "Mail Quantum" Lock + CommandPalette); `n0va\packages\frontend\src\pages\MailCollaboration.tsx` — Round 32 extended (4 collab2 stat cards, approval workflows card + modal, inbox delegation card + modal, team roles card + permission matrix, mentions card, 3 modals; NOTE: dynamic `api.adsMarketingModule[...]` indexing fails tsc — use explicit if/else branches)
- `n0va\packages\backend\src\services\MailIntegrationService.ts` — Round 33 extended (`mailIntegration`; OAuth 2.0 lifecycle: oauthStart w/ authorizationUrl + `st_` state + expiresInSeconds 600, oauthCallback w/ code + state-match + `oat_`/`rft_` tokens + ttl 50-99 min, oauthRefresh TTL 60-119 min that NEVER shortens existing expiry (`Math.max(now+ttl, oldExpiry)`), oauthRevoke, oauthStatus; 3 new actions share_link (`shr_` + shareUrl + permission view|comment)/update_crm_deal (proposal|negotiation|won)/create_doc (`doc_` + pageUrl); getConnectionPublic exposes oauthAuthorized/oauthState/oauthScope/tokenExpiresAt)
- `n0va\packages\backend\src\services\MailMigrationService.ts` (`mailMigration`) — Round 33 new service (MIGRATION_PROVIDERS 4 gmail/outlook/yahoo/imap × MIGRATION_MODES full/last_90_days/last_30_days; startMigration w/ deterministic `1200+hash%3800` × mode fraction + per-folder FOLDER_WEIGHTS summing to total; migrationScan w/ sizes + estimatedBytes; migrationPreview w/ 5 samples + folder mapping junk/bulk→spam + dedupeEstimate; runMigration w/ importCount `12+hash%18` + messageId dedupe + failCount `hash%3` + flags ["imported"] + importedFrom/migrationId/importedAt; migrationStatus progressPct computed (NOT hard 100) + remaining; list/delete/log/summary; collections `mail_migrations`, `mail_migration_log`)
- `n0va\packages\backend\src\__tests__\NovaMailRound15.test.ts` — Round 33, 30 tests all passing (deterministic; tenant `nova-mail15`; 6 describes: OAuth lifecycle 12 / new provider actions 6 / migration start 4 / scan+preview 3 / import 7; hashStr replicated to pin values; refresh-expiry max(old,new) + progressPct/remaining assertions; 5 TS18048 casts → zero new tsc errors)
- `n0va\packages\frontend\src\pages\MailImport.tsx` — Round 33 new page (route /mail/import; 4 stat cards from mailMigrationSummary, migration cards w/ status chips + progress bars + per-folder counts, New import modal w/ provider + source email + mailbox + mode, Scan/Preview/Import/Delete per status, preview modal w/ folder mapping chips + samples, providers breakdown + activity log; registered in App.tsx after /mail/quantum + Sidebar "Mail Import" Import icon + CommandPalette); `n0va\packages\frontend\src\pages\MailIntegrations.tsx` — Round 33 extended (OAuth modal w/ authorization URL + scopes + Complete authorization, KeyRound button per connected card, `OAuth ✓/pending/—` chips from oauthAuthorized/oauthState, token expiry + granted scope mono chips, ACTION_LABELS for share_link/update_crm_deal/create_doc)
- `n0va\packages\backend\src\services\MailThreadService.ts` (`mailThread`) — Round 34 service (threadList w/ filters state/tag/priority/unread/starred/pinned/search + mailboxId/folder, threadWorkspace w/ participants/messageCount/unreadCount/suggestedNext, setThreadState open|pending|done — done marks unread read, pin/unpin, tag/untag, priority, mergeThreads target+source re-thread, threadDashboard byState/byTag/byPriority/topTags, threadLog; collections `mail_threads`, `mail_threads_log`; NOTE: `MemoryStore.update` matches only the FIRST record — done-mark + merge use per-record `_id` updates); `n0va\packages\backend\src\services\MailIntegrationService.ts` — Round 34 extended (CONNECTORS 10→15 incl. google_sheets/asana/whatsapp/xero/drive + read_from_drive, categories 6→7 + finance; 6 new actions push_to_sheets/create_ticket/post_comment/send_sms/create_invoice/read_from_drive; body shape is `{action, params}` — the route passes `req.body.params` ONLY); `n0va\packages\backend\src\services\MailBillingService.ts` — Round 34 extended (COUPONS 4-code catalog w/ per-code remaining/maxUses, applyCoupon/removeCoupon/couponStatus, 10% off applied in buildInvoice after addons; DEFAULT_TAX_RATE 8, setTaxRate 0-30, tax = rate × discounted subtotal; coupon/tax persisted on `mail_billing` state)
- `n0va\packages\backend\src\__tests__\NovaMailRound16.test.ts` — Round 34, 34 tests all passing (deterministic; tenants `nova-mail16_{N}`; thread list/workspace/state/pin/tags/priority/merge/dashboard/log + 15-connector catalog + new action runs/rejections + coupon lifecycle + tax math; fixes during dev: `mail_billing` update no-ops → tenantState() before update in applyCoupon/setTaxRate; THR_B fixture uses `Date.now()-2h` for deterministic sort; dashboard tags asserted arrayContaining — 1-count ties; log asserted monotonic-sort + category membership; tax assert `total/11`)
- `n0va\packages\frontend\src\pages\MailThreads.tsx` — Round 34 new page (route /mail/threads; stat cards, filter bar w/ state/priority/tag selects + unread/starred/pinned checkboxes + subject search, thread rows w/ per-row state select/pin/priority/tag/merge, merge modal w/ source picker, workspace modal w/ messages + suggestedNext, activity log; registered in App.tsx after /mail/import + Sidebar "Mail Threads" GitMerge + CommandPalette); `n0va\packages\frontend\src\pages\MailIntegrations.tsx` — Round 34 ACTION_LABELS +6; `n0va\packages\frontend\src\pages\MailBilling.tsx` — Round 34 + Coupons & promo card (catalog w/ remaining/maxUses, apply/remove) + Tax rate card (0-30 input + save), Tag/Percent icons
- IMPORTANT (Round 34 route fix): legacy `GET /mail/threads/:threadId` (mailThread delegation) was registered BEFORE the Round-34 threads block and shadowed `/mail/threads/dashboard`, `/mail/threads/log` AND the workspace route (workspace returned legacy `msgs=1` shape, dashboard 500). Removed the duplicate at the messages section — the Round-34 block (dashboard/log/merge/workspace) is now the ONLY `/mail/threads*` definition. Do NOT re-add a bare `/mail/threads/:threadId` route earlier in the file.
- `n0va\packages\backend\src\services\MailBiometricService.ts` (`mailBiometric`) — Round 36 service (BIOMETRIC_SIGNALS 8, recordSample/baseline/evaluateSession/session lifecycle/biometricLog; collection `mailBiometric`); `n0va\packages\backend\src\services\MailZeroTrustService.ts` (`mailZeroTrust`) — Round 36 service (ZT_LAYERS 6 / ZT_PRINCIPLES 4, enforceAccess/enrollDevice/posture/honeytokens/zeroTrustOverview/Dashboard/Log); `n0va\packages\backend\src\services\MailAiGovernanceService.ts` (`mailAiGovernance`) — Round 36 service (MODEL_DOMAINS 4 + EXPORTED ETHICS_COMMITTEE 5, registerModel/reviewModel/guardrails/scanInput/scanOutput/shadowAi/redTeam/governanceDashboard/Log); `n0va\packages\backend\src\services\MailPerformanceService.ts` (`mailPerformance`) — Round 36 service (CACHE_LAYERS 9 / SCALABILITY_TARGETS 8 / EDGE_REGIONS / GREEN_CERTS, caching/flushCache/explainQuery/queryOptimization/scalability/edge/sustainability/performanceDashboard/Log); `n0va\packages\backend\src\services\MailChaosService.ts` (`mailChaos`) — Round 36 service (CHAOS_EXPERIMENTS 8 / GAME_DAYS 4, runExperiment/abortExperiment/resilienceScore/gameDays/chaosDashboard/chaosLog)
- `n0va\packages\backend\src\__tests__\NovaMailRound18.test.ts` — Round 36, 35 tests all passing (deterministic; tenants `nova-mail18`/`nova-mail18b`; imports ONLY exported names — `INJECTION_PATTERNS`/`TOXIC_PATTERNS`/`PII_PATTERNS` are module-private and corrupt the vitest import binding)
- `n0va\packages\frontend\src\pages\MailBiometrics.tsx` — Round 36 page (route /mail/biometrics; 4 stat cards, signals grid w/ deviation bars, baseline w/ risk level, session rows w/ trustScore chips, recent events); `n0va\packages\frontend\src\pages\MailTrust.tsx` — Round 36 page (route /mail/zero-trust; ZT score gauge, layer bars, principles, device rows, honeytokens, enroll + evaluate modals); `n0va\packages\frontend\src\pages\MailGovernance.tsx` — Round 36 page (route /mail/governance; model cards w/ register + approve/reject, guardrails, scanner w/ sample payloads, ethics committee, red-team); `n0va\packages\frontend\src\pages\MailPerformance.tsx` — Round 36 page (route /mail/performance; cache layers w/ hit-rate bars + flush, query explainer, edge regions, scalability, sustainability gauge); `n0va\packages\frontend\src\pages\MailChaos.tsx` — Round 36 page (route /mail/chaos; resilience banner, experiment catalog w/ per-card Run via `mailChaosRun`, run history, game days, events) — all registered in App.tsx (lazy, after /mail/admin) + Sidebar (Fingerprint/Shield/Scale/Gauge/Beaker) + CommandPalette
- `n0va\packages\backend\src\services\MailFocusService.ts` (`mailFocus`) — Round 37 service (FOCUS_MODES 5 incl. open-ended vacation / NOTIFICATION_TIERS 4 / BATCH_SUGGESTIONS 6; activateFocus w/ re-activation guard + duration validation, focusStatus w/ per-day stats + remainingMin, endFocus deterministic on tenantId|mode|suffix — NOT sessionId, extendFocus, notificationTiers, smartBatch, batchIntelligence + executeBatchSuggestions, frictionScore, roiCalculator, focusLog/focusDashboard; collections mail_focus_sessions, mail_focus_log, mail_friction)
- `n0va\packages\backend\src\__tests__\NovaMailRound19.test.ts` — Round 37, 24 tests all passing (deterministic; tenants nova-mail19/nova-mail19b/nova-mail19c — batch fixtures isolated on T3 so tier counts stay stable; pinned endFocus values via tenantId|mode seed; friction default 394/minimal/89%; ROI 12791% @1×)
- `n0va\packages\frontend\src\pages\MailFocus.tsx` — Round 37 page (route /mail/focus; activate form + active-session card w/ +30 min/End, 4 stat cards, T1-T4 tier grid, smart batching policy chips, batch intelligence catalog, friction score w/ recompute inputs, ROI calculator, recent activity log; registered in App.tsx after /mail/chaos + Sidebar "Focus Time" Target + CommandPalette); `n0va\packages\frontend\src\pages\MailInbox.tsx` — Round 37 pull-to-refresh gesture (touch handlers on root div + lg:hidden indicator strip, 60px threshold → loadAll)
- `n0va\packages\backend\src\services\MailComposerService.ts` (`mailComposer`) — Round 38 service (Neural Email Composer; COMPOSER_MODES 3 / COMPOSER_TONES 3 / COMPOSER_INTENTS 6; composerCatalog, composeDraft deterministic full-body generation, regenerateDraft, dislikeDraft, saveEdits, sendDraft 1-click via mailMessage.composeSend, listDrafts/getDraft/deleteDraft, composerDashboard, composerLog; collections mail_composer_drafts, mail_composer_log; determinism seed `${from.email}|${subject}|${mode}|${tone}|${variant}`)
- `n0va\packages\backend\src\__tests__\NovaMailRound20.test.ts` — Round 38, 19 tests all passing (deterministic; tenant nova-mail20 w/ 7 intent fixtures — mc_c5 body avoids the word "contract" so request-intent detection stays deterministic; 5 describes: catalog / zero-tap composeDraft / draft lifecycle / sendDraft / dashboard+log)
- `n0va\packages\frontend\src\pages\MailComposer.tsx` — Round 38 page (route /mail/composer; 4 stat cards, zero-tap compose form w/ inbox-message select + mode/tone selects, draft preview w/ ✅/⚠️ sections + basedOn + Send(1-click)/Regenerate/Edit/Don't-like, intent chips, drafts list w/ status chips + open/send/delete, recent activity, 30s auto-refresh; registered in App.tsx after /mail/focus + Sidebar "Mail Composer" PenLine + CommandPalette; NOTE: messages list unwraps `r.messages` NOT `r.data`)
